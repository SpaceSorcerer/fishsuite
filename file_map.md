# fishsuite — File Map

Standalone Python re-implementation of the Fiji image-analysis pipeline.

## Source

| Path | Description |
|---|---|
| `src/fishsuite/__init__.py` | Package init + bffile numpy<2 compatibility patch. |
| `src/fishsuite/cli.py` | 2026-06-19 ROTATION 'proper background' null (native port of the adversarially-validated rotation_null_prototype.py): `fishsuite backfill` gains a `--rotation` opt-in flag forwarded to backfill_run(do_rotation=). Click-based CLI: `run`, `preview`, `init`, `presets list/show`, `gui`. Entry point used by Brian. 2026-05-12: `gui` subcommand now launches the PySide6 GUI (`fishsuite.gui.main()`). 2026-06-18 POST-RUN SUBCOMMANDS (friendly wrappers over the standalone utility modules; the `python -m fishsuite.core.coloc_backfill`/`...walkthrough_figure` entry points still work unchanged): `backfill --run <run_dir> [--staging --input --seed --no-null-draws --no-radial --no-montage]` -> `coloc_backfill.backfill_run` (its --help states plainly CPU-only/no-GPU + reuses saved masks+spots); `walkthrough --run <run_dir> [--image --out --staging --input]` -> `walkthrough_figure.build_walkthrough_figure` (auto-picks image + default out `figures/07_coloc/79_pipeline_walkthrough.png`); `postrun --run <run_dir> [--staging --input --image --seed]` = one-shot "just make my figures" button that runs backfill THEN walkthrough with a per-step progress line + a final produced-files summary, continuing past a failed step and exiting non-zero. Staging/input auto-detected from `run_config.json` `input_dir` when omitted. Dispatch goes through module-level indirection helpers `_backfill_run`/`_build_walkthrough_figure` (lazy import; monkeypatch targets for the CLI tests). Expected/user-fixable errors (missing run_config/CSVs/masks, no staging) are translated by `_friendly_postrun_error` into plain-English actionable messages (e.g. "...pass --staging <path>") with NO traceback + exit code 2. All help/echo strings kept pure ASCII (cp1252 Windows-console safe). 2026-07-04 TWO MORE POST-RUN SUBCOMMANDS (same friendly-wrapper pattern; indirection helpers `_singlecell_run`/`_pixelpattern_run` are the monkeypatch targets; `_friendly_postrun_error` extended for a missing `nuclei_metrics.csv`): `singlecell --run <run_dir> [--abundance-col --group-a --group-b --include-secondary --no-figures --no-excel --out-subdir --seed]` -> `singlecell.singlecell_run` (CPU per-nucleus dose-response/matched-abundance/heterogeneity/saturation from nuclei_metrics.csv); `pixelpattern --run <run_dir> [--staging --input --secondary-match --no-figures --no-excel --out-subdir --seed]` -> `pixel_pattern.pixelpattern_run` (CPU per-nucleus pixel metrics + STAIN QC, reuses saved masks + re-reads raw like backfill). Both write into `<run>/deliverables/<out_subdir>/` and route group SuperPlots through the locked drawer (NT/control=orange, perturbation=blue). 2026-07-05 IF PUB-IMAGES SUBCOMMAND (same friendly-wrapper pattern; indirection helper `_if_pub_images_run` is the monkeypatch target; `_friendly_postrun_error` extended for a missing `per_well.csv`): `if-pub-images --run <run_dir> [--staging <single-plane raw dir> --zstack <z-stack dir> --source single_plane/picked_z (repeatable) --floor SEC=VALUE (repeatable) --ceiling-pct --scalebar --label]` -> `if_pub_images.regenerate_pub_images` (CPU-only; NO GPU / NO re-segment; rebuilds the plate from per_well.csv, FOV counts from per_fov.csv, source dirs/floors/label default from `if_run_context.json`). Writes `<run>/publication_images/`. 2026-07-05 FULL DISPLAY-RANGE CONTROL (back-compat; all new flags optional, defaults reproduce prior renders): `--floor` now accepts an optional `:source` (`647:single_plane=5000`, `647:picked_z=5500`; bare `647=5000` still = both sources, more-specific key wins); new `--ceiling SEC=VALUE`/`SEC:source=VALUE` (explicit vmax that overrides `--ceiling-pct` for that secondary/source; unset pairs keep the WT-primary percentile); new `--dapi-floor`/`--dapi-ceiling` (FIXED DAPI vmin/vmax across ALL panels — e.g. `--dapi-ceiling 8000` so DAPI isn't over-exposed; unset = per-image DAPI); new `--per-image-ceiling` flag (each panel's ceiling from ITS OWN percentile — non-rigorous fallback, breaks cross-panel comparability, explicit `--ceiling` still wins; default OFF). Shared parse helper `_parse_range_opt` validates `SEC=VALUE`/`SEC:source=VALUE` and the `single_plane`/`picked_z` source token (plain-English exit-2 on malformed input). |
| `src/fishsuite/gui/__init__.py` | NEW (2026-05-12). Package shim re-exporting `main` from `gui.main` so `from fishsuite import gui; gui.main()` works exactly as before. |
| `src/fishsuite/gui/__main__.py` | NEW (2026-05-12). Allows `python -m fishsuite.gui [--print-toolkit]` as a module-script invocation. |
| `src/fishsuite/gui/main.py` | 2026-05-18 pub-contrast UI pass: Output tab gained a "Save TIFs alongside PNGs" checkbox (default off) wired to `OutputCfg.save_publication_tifs`, a "Publication-image contrast" subsection with a `QComboBox` for `pub_contrast_mode` (`auto_batch` / `auto_per_image` / `manual`), four `QDoubleSpinBox` percentile knobs (FISH floor/ceil + DAPI floor/ceil) enabled only in auto modes, and a `QGroupBox` "Manual contrast" subpanel with a 4×2 grid of `QDoubleSpinBox` (DAPI / RNA1 / RNA2 / Protein × Min / Max) visible only when mode is `manual`. The manual spinboxes use -1.0 as the "leave blank / inherit per-image percentile" sentinel (round-trips ↔ `Optional[float] None` in the schema). New `_on_pub_contrast_mode_changed(mode)` class method drives the visibility toggle. `_apply_cfg_to_widgets` and `_read_widgets_into_cfg` extended to read/write all new `OutputCfg` keys including the manual min/max pairs. 2026-05-13 channel-detect + per-file-tree pass (Brian: "i can not select which channel is which rna ... and we need to be able to do subfolders and select specific files individually"): added "Detect channels from sample file" button on the Channels & Mode tab — opens the first non-sec-only image in `input_dir` via `bioio` and surfaces (a) a read-only multi-line metadata block ("Ch 0: 640 CSU / Cy5/AF647 (em ~668 nm)"), (b) a per-role italic "→ Detected name" label next to every channel-index spinner (`chan_dapi_resolved`, `chan_rna_resolved`, `chan_rna2_resolved`, `chan_ab_resolved`, `chan_ab2_resolved`) that updates live when the user changes the spinner. Persists last-detected layout to `~/.fishsuite_gui.json` under `last_detected_channels` so the user still sees context on reopen before re-running Detect. Added a per-file checkable `QTreeWidget` to the Experiment & Paths tab: subfolders surfaced as parent items, leaves are `.vsi` / `.czi` / `.tif` / `.tiff` / `.lif` / `.nd2` / `.oib` / `.oif` files (1-level deep). Live "Included: N of M files (KO: A, WT: B, Sec-only: C excluded)" summary chip above the tree. Buttons: Refresh, Include all, Include none, Exclude all sec-only, plus a substring filter line edit. Right-click context menu per leaf: "Include only this", "Exclude this", "Mark as sec-only" (adds to `cfg.conditions.sec_only_files`), "Include all in this folder", "Exclude all in this folder". Per-leaf visual: unchecked rows render in grey (`#9aa0a6`); checked rows use the palette default — instant visual distinction. Per-input-dir selections persist via `state.file_subset_by_input` (one list per input directory). When the saved subset equals every discovered file, the runtime cfg emits an empty `input_file_subset` so the runner falls back to legacy "include all" behavior. Validated on BIN1 (`F:\Raw Images\BIN1\20251029_d8cMyo_BIN1-RNAFISH\...`): tree lists KO (6 files) + WT (6 files), detect button correctly returns the 4-channel Olympus VS200 metadata (`['640 CSU', '561 CSU', '488 CSU', '405 CSU']` with 130 nm/210 nm XY/Z voxels), the rna_only resolved labels read "→ Ch 3: 405 CSU / DAPI" and "→ Ch 0: 640 CSU / Cy5/AF647 (em ~668 nm)". 2026-05-13 BIN1 Run1 issue 4 (color selection): added per-channel "Color (LUT)" QComboBox column in the "Channels & Mode" tab. Five new combos (`chan_dapi_lut`, `chan_rna_lut`, `chan_rna2_lut`, `chan_ab_lut`, `chan_ab2_lut`) populated with the 9-name LUT list (blue/yellow/cyan/magenta/green/red/orange/gray/fire); defaults match historical hard-coded colors. New `_make_lut_combo(default_name)` helper. `_chan_rows` is now a 4-tuple (label, spin, label_edit, lut_combo); `_refresh_channel_visibility` toggles the LUT combo visibility along with the rest of the channel row. Visibility follows the same mode-gated rules as the channel-index field. Settings round-trip via the channels dict (`dapi_lut`/`rna_lut`/`rna2_lut`/`antibody_lut`/`ab2_lut`) into YAML and via the schema into `save_publication_images_bundle(..., dapi_lut=..., rna_lut=..., rna2_lut=..., antibody_lut=...)`. 2026-05-13 per-channel-overrides pass: the Foci / Spots tab gained a new "RNA2 BigFISH overrides" QGroupBox (visible only when `analysis_mode == "rna_rna"` AND backend == "bigfish"). A top-row "Use same settings as RNA1" checkbox toggles the override widgets enabled/disabled — when checked (default), the YAML emits empty `rna2_overrides:` (every field None → inherits from FociCfg); when unchecked, the YAML emits explicit per-RNA2 values for `bigfish_spot_radius_nm`, `bigfish_spot_radius_z_nm`, `threshold_multiplier`, `only_nuclear_spots`, `min_sep_px`. Also added a shared `min_sep_px` QSpinBox to the BigFISH group (round-trips for Fiji NMS parity). `_refresh_foci_visibility` + `_on_mode_changed` now coordinate to show/hide the RNA2 group reactively. 2026-05-13 channel-labels pass: the "Channels & Mode" tab gained a per-channel **Label** QLineEdit column next to each channel-index spinbox (`chan_dapi_label`, `chan_rna_label`, `chan_rna2_label`, `chan_ab_label`, `chan_ab2_label`). Placeholders show the schema defaults (DAPI, RNA1, RNA2, Protein, Protein2); blanks fall back to those defaults at save time. Visibility of the label fields follows the same mode-gated rules as the matching channel-index field. The live channel preview ("Ch 0 = ...") now uses the user-typed label so it reads e.g. "Ch 0 = MIAT-Cy5" instead of the generic role name. PySide6 main window — full 11-tab launcher (Experiment & Paths, Conditions, Channels & Mode, Z-Stack, Nuclei, Foci/Spots, Pixel Coloc, Cytoplasm, Output, Run, YAML Preview) with colored readiness dots in each tab label, dynamic backend-specific option groups (StarDist vs Cellpose vs Otsu, BigFISH vs LoG, MAD vs percentile), live YAML preview, run-progress parsing, history list, theme switcher (Light/Dark/System), quick-overlay dropdown (Strict/Permissive spots, k_mad sweeps, StarDist prob), Ctrl+R / Esc keyboard shortcuts, Ctrl+1..0 tab nav. Spawns `fishsuite.cli run` + downstream `analysis.single_condition_plots` via the subprocess runner; settings persist to `~/.fishsuite_gui.json`. 2026-05-13 readability pass: replaced inline `setStyleSheet` colour overrides with object-name selectors so widgets follow the active theme; added `_apply_palette` (sets QPalette so non-QSS widgets like QFileDialog/QMessageBox stay readable in dark mode); added `_apply_theme` + 3-state combo (Light / Dark / System auto with luminance heuristic); the app now forces Fusion style on startup (Win11 native style was ignoring QSpinBox and combo-popup rules). 2026-05-13 combo audit pass (Brian: "we need dropdowns for any drop down selectable not re typing it out"): converted the two remaining free-text enum-shaped fields to editable `QComboBox` so users pick from a known-good list instead of retyping. `sd_model` (StarDist `stardist_model`) is now a combo of `[2D_versatile_fluo, 2D_paper_dsb2018, 2D_versatile_he, 2D_demo]`; `cp_model` (Cellpose `cellpose_model_type`) is now a combo of `[cpsam, nuclei, cyto, cyto2, cyto3]` — the legacy 2/3 names are listed for back-compat (presets `h9_hesc_100x.yaml` and `bin1_d8cmyo_100x.yaml` use `nuclei`). Combos are editable so a user-trained model name still round-trips. New `_make_open_combo(items, default, tooltip)` helper and `_set_open_combo(cb, value)` setter that auto-inserts unknown values from a loaded YAML so they remain selectable. Ported Fiji-GUI tooltips onto the existing fixed-enum combos (`mode_combo`, `z_mode`, `nuc_backend`, `sd_postprocess`, `foci_backend`, `cond_mode`, `pc_mode`, `pc_scope`) so hovering each dropdown shows what every option does. Ported Fiji's sec-only-individual-file picker UX: new `sec_files_box` QGroupBox on the Conditions tab with `+ Add files…` / `Remove selected` buttons, populates `conditions.sec_only_files` (schema field already existed but was always emitted empty). Visibility gated on `conditions.mode == "subfolders"` to match `sec_box`. Intentionally skipped: porting Fiji's `FIELD_VISIBILITY` declarative table (current scattered `setVisible` calls work and converting would risk regressions on the readiness logic); porting `auto_detect_channels` button (Fiji helper not available in fishsuite — runtime auto-detect via `-1` already works); profile dropdown (fishsuite already has `_preset_combo` + `_overlay_combo` which together cover the same UX surface). |
| `src/fishsuite/gui/readiness.py` | NEW (2026-05-12). Pure-logic Qt-free readiness evaluator — given a config dict + paths/tag, returns per-tab "red"/"yellow"/"green" status. Drives both the tab dot icons and the Run-button enabled state. Self-check (`python -m fishsuite.gui.readiness`) covers 4 scenarios. |
| `src/fishsuite/gui/state.py` | Preset I/O (`list_presets`, `load_preset`, `save_preset_as`), schema-defaults extraction, deep-merge for overlays, output-dir resolution from base+tag+timestamp, and `~/.fishsuite_gui.json` settings persistence (last preset, last paths, last overrides, theme, window geometry, recent-run history). 2026-05-13: added tri-state `theme` setting (`system`/`light`/`dark`) + `resolve_theme()` helper that falls back to the legacy `dark_mode` bool when no explicit `theme` key is present so existing user settings keep working. 2026-05-13 channel-detect + per-file-tree pass: added `last_detected_channels` (dict, persists last metadata scan) + `file_subset_by_input` (dict mapping `input_dir -> [relative_file, ...]`) entries to `DEFAULT_SETTINGS`. New Qt-free helpers: `fluorophore_for_channel_name(name)` heuristic (maps "640 CSU"/"561 CSU"/"488 CSU"/"405 CSU"/keyword variants → fluorophore + emission peak), `format_channel_metadata_row(name)` (one-liner display), `detect_channels_from_file(file_path)` (opens via `bioio`, returns `{names, n_channels, voxel_xy_nm, voxel_z_nm, source_file, error}` dict — pure helper, no Qt), `scan_input_dir_tree(input_dir, max_depth=2)` (walks one level deep, returns `{root, subfolders: [{name, rel, files: [{name, rel}, ...]}, ...], root_files}` — used to build the per-file QTreeWidget). `TREE_EXTENSIONS` constant: `(.vsi, .czi, .tif, .tiff, .ome.tif, .lif, .nd2, .oib, .oif)`. |
| `src/fishsuite/gui/runner_proc.py` | NEW (2026-05-12). Qt-aware subprocess manager — spawns the pipeline + optional downstream child process, drains stdout in a daemon thread, parses rich progress markers ("[3/12] H9-NT..." and "Pre-pass: 5/12") into `progress(int,int,str)` signals, exposes `line`/`progress`/`finished`/`phase_changed` Qt signals to the UI. |
| `src/fishsuite/gui/widgets.py` | Small Qt widgets used across tabs: `StatusDot`, `SectionHeader`, `SliderSpin`/`IntSliderSpin`, `FolderConditionTable`, `ReorderableList`. Light + dark Qt stylesheets built by `_build_qss(dark=...)` (unified builder — every background-setting rule has a paired foreground colour, WCAG-AA contrast targets). 2026-05-13 readability pass: rewrote LIGHT_QSS / DARK_QSS to a single helper, explicitly styled QComboBox popup (`QAbstractItemView`), QMenu, QToolTip, QScrollBar, disabled state, and the `previewChip`/`outputPreview`/`runOutputPath`/`footerStatus[state=...]`/`yamlView` object-name selectors used by main.py. Body font bumped 10→11pt, section header 11→12pt. Added theme-aware helpers `muted_color(dark)`, `divider_color(dark)`, `accent_bg(dark)`, `panel_bg(dark)` exposed to callers. StatusDot outline thickened (visible against dark tab bar). |
| `src/fishsuite/runner.py` | 2026-06-19 ROTATION 'proper background' null (native port of the adversarially-validated rotation_null_prototype.py): accumulate `res.extra['coloc_rotation_null']` per image and write `coloc_rotation_null.csv` (default OFF -> no file). 2026-06-10 ADDITIVE reproducibility/QC hardening: (a) at the VERY START of `run_batch` (right after `cfg = FishsuiteConfig.from_yaml`) calls `core.repro.set_global_seeds(cfg.seed)` and logs the seeded-backends dict, wrapped in try/except so a seeding failure can never abort a run (cfg.seed default 0; complementary to `foci.partner_null_seed`). (b) right after `output_dir.mkdir`, calls `core.repro.write_run_metadata` to emit `versions.txt` + `command.log` into the run output dir (crash-proof, warn-not-abort). (c) after `res = mode_fn(...)` and BEFORE `per_image_rows.append(res.per_image)`, merges `core.qc.compute_qc_flags(res, cfg)` into `res.per_image` via `dict.update()` (purely additive new `qc_*` columns; crash-proof; never drops/excludes an image). All three are gated by try/except and default-neutral config — legacy presets produce byte-identical analysis numbers (only the additive provenance files + advisory qc_* columns appear). 2026-06-05 honest-threshold-panel wiring: the rna_rna/rna_protein walkthrough call now computes `_wk_floor_is_gate = bool(cfg.output.apply_pub_contrast_floor_to_spots)` and forwards it as `floor_is_spot_gate=` to `save_walkthrough_bundle_rna_rna`. True (BIN1 rna_rna/rna_protein presets) keeps the legacy floor-mask step05/06 panels; False (MIAT-QKI) makes channels-with-spots render the actual BigFISH-LoG called spots instead of the misleading low-display-floor flood. No other call site touched. 2026-06-02 thresholded-RNA-intensity wiring: the `analysis_floors` kwarg is now ALWAYS forwarded for the supported modes (rna_only/rna_rna/rna_protein), not only when `apply_pub_contrast_floor_to_*` is on — so the new thresholded-intensity feature can default its floor to the resolved spot floor with the toggles off (modes treat a missing floor as NaN columns; schema-stable). 2026-06-02 walkthrough spot-floor wiring: the rna_only `save_walkthrough_bundle` call (else-branch, ~line 1724) now computes `_wk_spot_floor` = `cfg.output.manual_rna_min` when `cfg.output.apply_pub_contrast_floor_to_spots` is True and the floor > 0 (else None), and forwards it as `spot_floor=`. This makes future rna_only runs render the step05/06/08 threshold panels at the real spot gate (e.g. 800) rather than the lower pixel-coloc MAD mask. No effect when the spot floor is inactive. 2026-05-20 manual-mode `batch_contrast` population pass: when `output.pub_contrast_mode == "manual"`, the runner now mirrors `manual_<channel>_min/max` into `batch_contrast["rna"/"rna2"/"dapi"/"antibody"]` AFTER the auto_batch / reference_image pre-scan branches (which are skipped in manual mode) and BEFORE the per-image processing loop. This fixes a latent bug where the `analysis_floors` lookup (rna_rna mode kwargs) read from an empty `batch_contrast` and forwarded `None` floors → every `*_above_floor_intensity_*` per-nucleus column came out NaN and the `apply_pub_contrast_floor_to_spots` filter no-op'd, even with both `apply_pub_contrast_floor_to_analysis` and `apply_pub_contrast_floor_to_spots = True` in the preset. Per-image manual PNG render branch (line ~1172) was untouched — it already reads `cfg.output.manual_*` directly, so the publication images were never affected by the bug. The new block is verbatim user-typed values with NO bump (the auto_batch RNA floor bump is reserved for percentile-derived floors). 2026-05-18 pub-contrast batch pre-scan pass: when `output.pub_contrast_mode == "auto_batch"` (the new default), `run_batch` runs a pre-pass over every non-sec-only image that pools RAW pixels per channel (DAPI / RNA / RNA2 / antibody) using the same z-mode/start/end as the per-image pass, then computes ONE `(floor=p<floor_pct>, ceil=p<ceil_pct>)` per channel from `cfg.output.pub_contrast_floor_pct` / `_ceil_pct` (FISH) or `_dapi_floor_pct` / `_dapi_ceil_pct` (DAPI). Sec-only images are EXCLUDED from the pool (their dim background would shrink the ceiling) but still get rendered with the same uniform (floor, ceil) so they appear correctly dim. Values are stored in `batch_contrast: Dict[str, tuple[float,float]]` and threaded into every `save_publication_images_bundle` call as `dapi_floor=/dapi_ceil=`/`rna_floor=`/etc absolute overrides. In `manual` mode the pre-scan is skipped and the per-channel `cfg.output.manual_<channel>_min/max` values flow through as overrides verbatim; in `auto_per_image` mode no overrides are sent and the renderer falls back to per-image percentiles (legacy behavior). `run_config.json` gains `PUB_CONTRAST_MODE`, `PUB_CONTRAST_FLOOR_PCT`, `PUB_CONTRAST_CEIL_PCT`, `PUB_CONTRAST_DAPI_FLOOR_PCT`, `PUB_CONTRAST_DAPI_CEIL_PCT`, `SAVE_PUBLICATION_TIFS`, and a `batch_contrast` block recording the actual per-channel (floor, ceil) applied. The pub-image call also forwards `save_tifs=cfg.output.save_publication_tifs` so PNG-only renders skip the .tif emission. 2026-05-15 walkthrough audit pass: pipeline_walkthrough block now ALWAYS runs (was guarded by `not dimg.sec_only` — sec-only no-probe controls now also get walkthroughs, with sec_only=True propagated so the batch contrast cache stays clean). The block now threads `qc["cyt_labels"]`, `res.spots` (rna_only), and `qc["spots1"]/spots2` (rna_rna) into `save_walkthrough_bundle(_rna_rna)` so the new step07/08/09/10/11 panels can render. Also wires `cfg.channels.rna2_lut` through `render_all_in_one_qc_rna_rna` + `save_walkthrough_bundle_rna_rna` so the QC overlay + walkthrough render RNA2 in the same magenta/cyan as the publication images. 2026-05-14 auto-downstream pass: after run_config.json is written, the runner now spawns `python -m analysis.single_condition_plots --output-dir <out>` in `F:\Image Analysis Work\image-analysis-pipeline\python\` as a subprocess (best-effort, log to `<out>/_downstream_plots.log`). Mirrors the Fiji `run_downstream_analysis` hook so CLI runs always produce `figures/` alongside the CSVs. 2026-05-13 per-file-subset filter (Improvement 2 from channel-detect + per-file-tree pass): after `discover_inputs` and before the batch pre-pass, `run_batch` now applies `cfg.input_file_subset` (empty list = include all). Match rule: each subset entry can be a bare basename (matched against `Path.name`), a forward-slash relative path (matched against `Path.relative_to(input_dir).as_posix()`), or an absolute path (matched against `Path.as_posix()`); the runner normalises both sides. Logs "found N images, input_file_subset filter -> kept K of N". When a non-empty subset matches zero discovered files, raises a `RuntimeError` with the first 5 subset entries echoed for debugging. Empty list preserves legacy back-compat exactly. 2026-05-13 BIN1 Run1 issue 4 (color selection): `save_publication_images_bundle` call now forwards the per-channel LUT names from `cfg.channels.{dapi,rna,rna2,antibody}_lut` so the user-selected color reaches the renderer. 2026-05-13 channel-labels pass: per-image threshold rows are now stamped with `dapi_label` / `rna_label` (and `rna2_label` / `antibody_label` / `ab2_label` for modes that use those channels) so both the master `thresholds.csv` AND every `masks/<stem>__thresholds.csv` carry the human channel names; QC-overlay + publication-image dispatch now forwards the user-typed labels into `render_all_in_one_qc(_rna_rna)` + `save_publication_images_bundle` so filenames and the burned-in legend reflect the YAML labels; `run_config.json` gains top-level `CHANNEL_DAPI_LABEL` / `CHANNEL_RNA_LABEL` / `CHANNEL_RNA2_LABEL` / `CHANNEL_ANTIBODY_LABEL` / `CHANNEL_AB2_LABEL` keys. End-to-end batch runner. Walks input dir, dispatches per-image work to the analysis mode, then writes the full Fiji-compatible output dir layout (qc_overlays/, per_image_csv/, masks/, pipeline_walkthrough/, nuclei_popouts/, publication_images/, plus master CSVs + xlsx + run_config.json). 2026-05-12 batch-scope fix: when `pixel_coloc.threshold_scope == "batch"` the runner now performs a pre-pass over all images that pools raw nuclear RNA pixels and computes ONE median+k*MAD threshold, then passes it to every per-image call (matches Fiji's `run_batch_prescan_for_thresholds`). 2026-05-12 output-fixes pass: (a) every per-image output filename now embeds the sanitized condition label via `_stem_with_condition` (`<stem>__<COND>__<suffix>`), (b) images are processed non-sec-only-first so the batch contrast cache is populated before any sec-only renders, (c) `sec_only` is forwarded to `render_all_in_one_qc` / `save_publication_images_bundle` so sec-only no-probe controls consult-but-do-not-update the running-max contrast cache. 2026-05-13 rna_rna pass: the batch pre-scan now dispatches on `analysis_mode == "rna_rna"` to `rna_rna.collect_nuclear_rna_pixels` (returns a tuple `(rna_pixels, rna2_pixels)`) and pools each channel SEPARATELY, producing two batch thresholds (`batch_rna_threshold`, `batch_rna2_threshold`); both are forwarded as kwargs to the per-image call. Rendering paths gained a two-channel branch: when `qc["rna2_2d"]` is present, the runner writes `__qc_dapi_rna1_rna2_nuclei_spots.png`, calls `save_publication_images_bundle(..., rna2=...)`, uses `save_walkthrough_bundle_rna_rna(...)`, and writes `__spot_mask_rna2.tif` + per-channel spot CSVs (`__spot_metrics_rna1.csv` + `__spot_metrics_rna2.csv`). 2026-07-03 IF-intensity divert: a single guard right after `write_run_metadata` returns `core.modes.if_intensity.run_if_batch(cfg, config_path, input_dir, output_dir, dirs, ...)` when `channels.analysis_mode == "if_intensity"` — placed BEFORE `discover_inputs`/the per-image loop, so every FISH mode's code path below is byte-for-byte untouched (lazy import keeps package import unaffected). 2026-07-05 RUN-LEVEL OVER-DETECT QC: after `per_image_rows` is fully collected and BEFORE `pd.DataFrame(per_image_rows)`, calls `core.qc.flag_overdetect_outliers(per_image_rows, cfg)` (crash-proof try/except; logs the count flagged). It seeds `qc_overdetect_rna1_run_outlier` on every row and flags RNA1 spots/nucleus robust-outliers vs the run median — advisory only, never drops/reorders an image. Complements the per-image absolute cap already merged via `compute_qc_flags`. |
| `src/fishsuite/config/schema.py` | 2026-06-19 ROTATION 'proper background' null (native port of the adversarially-validated rotation_null_prototype.py): FociCfg gains DEFAULT-OFF flags `compute_partner_rotation_null` (+`partner_rotation_n`=1000, `partner_rotation_seed`=0, `partner_rotation_min_retention`=0.5, `partner_rotation_assoc_percentile`=95.0, `compute_partner_translation_null`, `save_partner_rotation_null_draws`); all default OFF -> byte-equivalent output. Pydantic v2 schema for the YAML config. Mirrors the Fiji-pipeline schema where keys overlap. 2026-06-10 ADDITIVE reproducibility/QC: added a new `QcCfg(BaseModel)` block (`qc_min_nuclei: int = 5`, `qc_saturated_frac: float = 0.01`, `qc_min_focus_score: float = 0.0`) wired onto `FishsuiteConfig` as `qc: QcCfg = Field(default_factory=QcCfg)`, plus a top-level `FishsuiteConfig.seed: int = 0` (broad global reproducibility seed for `core.repro.set_global_seeds`; explicitly COMPLEMENTARY to — not a replacement for — `foci.partner_null_seed`). All new fields default to neutral/OFF so legacy YAMLs load unchanged and `foci.partner_null_seed` is untouched. 2026-06-05 SCIENTIFIC-CORE (per-nucleus random-position null coloc): added 5 GATED `FociCfg` fields after `detect_antibody_spots` — `compute_partner_null_enrichment: bool = False` (L437; master gate, requires `compute_partner_intensity`; default OFF -> no columns, byte-identical), `partner_null_n: int = 1000` (L439), `partner_null_disk_px: float = 3.0` (L446; CANONICAL disk radius = the validated reference-script value, intentionally decoupled from the tiny `compute_partner_intensity` radius which is ~1 px at the MIAT-QKI 100 nm-radius/0.13 µm-voxel preset and would NOT reproduce the method), `partner_null_seed: int = 0` (L448; deterministic RNG), `exclude_nucleolus_from_partner_null: bool = False` (L456; with `nucleolus.enabled`, removes DAPI-poor nucleolar voids from BOTH the random-null positions and the observed rna1 spots). All default OFF so existing BIN1/H9/rna_rna runs are byte-identical. 2026-06-05: added `FociCfg.detect_antibody_spots: bool = True` (right after `compute_partner_intensity`) — RNA_PROTEIN MODE ONLY opt-in. Default True = existing behavior byte-identical (antibody/protein channel BigFISH-detected as before). When False the antibody (rna2-slot) channel is treated as a DIFFUSE INTENSITY channel: spot detection is SKIPPED (empty spot set), so a diffuse antibody stain (QKI IF) no longer carpets the field; the intensity-based coloc (`compute_partner_intensity` samples antibody PIXELS at rna1 spots) + pixel-coloc (Pearson/Manders/Li on the antibody pixel plane) are UNAFFECTED. Plain rna_rna (two real FISH targets) ALWAYS detects both channels regardless of this flag (it is consulted only when the rna2 slot is the antibody — see rna_protein.py / rna_rna.py `rna2_is_antibody`). Replaces the old `antibody_overrides:{threshold_multiplier:50,only_nuclear_spots:true}` ~zero-spots hack. 2026-06-02: added `OutputCfg.rna_intensity_threshold: Optional[float] = None` + `rna2_intensity_threshold: Optional[float] = None` — settable floor for the new thresholded-RNA-intensity-in-compartments feature. When None/<=0 the floor DEFAULTS to the spot floor (runner-forwarded pub-contrast floor / `manual_<ch>_min`); set positive to pin explicitly. Applied to the spot-detection RNA plane; see `THRESHOLD_INTENSITY_FEATURE.md`. 2026-05-31: added `ConditionsCfg.filename_conditions: List[List[str]] = []` — ordered `[substring, condition]` pairs for flat-folder filename-encoded condition assignment (default empty = legacy; consumed by `discover_inputs` via the runner). Also `ZStackCfg.focus_central_fraction` (see autofocus_maxproj note below). 2026-05-18 pub-contrast knobs pass: extended `OutputCfg` with the publication-image contrast strategy + manual override fields — `save_publication_tifs: bool = False` (PNG-only by default; flip on for 16-bit RGB TIFs alongside the PNGs), `pub_contrast_mode: Literal["auto_batch","auto_per_image","manual"] = "auto_batch"` (auto_batch pre-scans the run + applies ONE per-channel floor/ceil to every image; auto_per_image is the legacy per-image-percentile behavior; manual uses the typed min/max numbers below verbatim), `pub_contrast_floor_pct=98.0` / `pub_contrast_ceil_pct=99.9` (FISH-channel percentiles for auto modes), `pub_contrast_dapi_floor_pct=10.0` / `pub_contrast_dapi_ceil_pct=99.9` (DAPI percentiles — separated because DAPI floor is fundamentally lower than FISH), and four `Optional[float]` manual min/max pairs (`manual_dapi_min`/`_max`, `manual_rna_min`/`_max`, `manual_rna2_min`/`_max`, `manual_antibody_min`/`_max`; None on either half falls back to per-image percentile for that half so callers can pin just a floor or just a ceiling). Mirrors Fiji's `PUB_IMG_CONTRAST_MODE` + `PUB_IMG_*_MIN/MAX` knobs at `F:\Image Analysis Work\image-analysis-pipeline\python\gui_pipeline.py:728+`. 2026-05-13 per-file-subset: added top-level `FishsuiteConfig.input_file_subset: List[str]` (default `[]` = include all discovered files, legacy back-compat). When non-empty, the runner filters its discovered file list to only the matching entries; values may be bare basenames, posix-relative paths, or absolute paths (runner normalises both sides). Populated from the GUI's per-file QTreeWidget. 2026-05-13 BIN1 Run1 issue 4: added five per-channel LUT (pseudo-color) fields to `ChannelsCfg` (`dapi_lut="blue"`, `rna_lut="yellow"`, `rna2_lut="cyan"`, `antibody_lut="magenta"`, `ab2_lut="green"`). Defaults match the historical hard-coded colors so legacy YAML configs render unchanged. Accepted values: blue / yellow / cyan / magenta / green / red / orange / gray / fire (case-insensitive). 2026-05-13: added `NucleiCfg.label_smoothing_radius_px` (default 0 = disabled); added `SpotColocCfg` block (`pair_distance_um=0.3`, `report_nn_distance=True`) for rna_rna spot-to-spot pairing. 2026-05-13 labels pass: added five user-typed channel-label fields to `ChannelsCfg` (`dapi_label="DAPI"`, `rna_label="RNA1"`, `rna2_label="RNA2"`, `antibody_label="Protein"`, `ab2_label="Protein2"`). Defaults match the historical role names so legacy YAMLs render byte-identical filenames. 2026-05-13 per-channel-overrides pass: added `FociChannelOverrideCfg` (all-Optional fields: `bigfish_spot_radius_nm`, `bigfish_spot_radius_z_nm`, `threshold_multiplier`, `only_nuclear_spots`, `min_sep_px`) and wired two instances onto `FociCfg` as `rna_overrides` + `rna2_overrides`. Added shared `FociCfg.min_sep_px=1` (round-trips for Fiji NMS parity; no-op on fishsuite side). New helper `FociCfg.resolved_for(channel: 'rna' | 'rna2') -> dict` returns the effective spot-detection params after applying the relevant override on top of the shared values. When an override field is None the shared FociCfg value is used → legacy YAMLs without `rna2_overrides` keep identical behavior. 2026-05-29 ghost-nucleus fix: added three OPT-IN fields to `NucleiCfg` — `reject_ghost_nuclei: bool = False` (default OFF → every other dataset/preset byte-for-byte unchanged), `reject_ghost_max_dapi_cv: float = 0.12`, `reject_ghost_min_area_px: int = 6000`. When True, the rna_only mode drops empty 'ghost' oval shells via the post-detection composite rule in `core.segmentation.identify_ghost_nuclei`. 2026-06-06 NATIVE coloc-figure outputs: added 3 GATED `FociCfg` fields AFTER `exclude_nucleolus_from_partner_null` — `save_partner_null_draws: bool = False` (surfaces the per-image pooled null vector + pooled observed via `extra["coloc_null_draws"]`; requires `compute_partner_null_enrichment`), `compute_partner_radial_profile: bool = False` (per-ring radial QKI-around-MIAT profile via `extra["coloc_radial_profile"]`; requires `compute_partner_intensity`), and `partner_radial_bins_um: List[float] = [0.25,0.5,0.75,1.0]` (outer-edge radii µm of the concentric annuli). All default OFF/empty-carrier -> emitted ONLY via the empty-default `ImageResult.extra` dict -> per_image/nuclei/spots tables byte-identical (BIN1/H9 unaffected). 2026-07-03 IF-intensity mode: added `"if_intensity"` to the `ChannelsCfg.analysis_mode` Literal and a new `IfIntensityCfg` block wired onto `FishsuiteConfig` as `if_intensity: IfIntensityCfg` (plate_layout_csv, well_secondary_overrides, signal_channel_map, dapi_channel_key, cyto_ring_px, exposure_tol_s, display_ceiling/floor_pct, scalebar_um, micrograph_source/zstack_dir/z_window_frac, fig_seed, make_excel). All neutral/OFF for other modes — legacy YAMLs load unchanged. 2026-07-05 PUB-IMAGES knobs added to `IfIntensityCfg`: `pub_images`(True), `pub_sources`(["single_plane","picked_z"]), `pub_display_floors`({"647":5000,"568":3500}; keys accept `"SEC:source"` per-source scoping), `pub_ceiling_pct`(99.5), `pub_zstack_dir`(""→falls back to micrograph_zstack_dir), `pub_z_central_frac`(0.8), `pub_dapi_weight`(0.6), `pub_signal_label`("signal"). Drive the native `if_pub_images` renderer; defaults keep every other mode unaffected. 2026-07-05 FULL DISPLAY-RANGE CONTROL: added 4 fields to `IfIntensityCfg` — `pub_display_ceilings: Dict[str,float] = {}` (EXPLICIT vmax override per secondary and/or `"SEC:source"`; overrides `pub_ceiling_pct` where present, else WT-primary percentile), `pub_dapi_floor: Optional[float] = None` + `pub_dapi_ceiling: Optional[float] = None` (FIXED DAPI vmin/vmax across all panels; None = per-image DAPI min-max; one-sided allowed), `pub_per_image_ceiling: bool = False` (each panel's ceiling from its own percentile — non-rigorous fallback, explicit ceiling still wins). All default empty/None/False → byte-identical legacy renders; persisted into `if_run_context.json` for GPU-free regeneration. 2026-07-05 RNA-ANCHORED AUTOFOCUS + OVER-DETECT QC: `ZStackCfg` gains `autofocus_channel: Literal["dapi","rna","auto"] = "dapi"` (default = unchanged; `"rna"` picks the sharpest RNA1 plane and locks all channels to it; `"auto"` RNA-anchors when the RNA dynamic-range clears the threshold else DAPI-anchors) + `autofocus_auto_rna_quality_min: float = 3.0` (the auto gate on RNA `(p99.9-median)/(1.4826*MAD)`). `QcCfg` gains `qc_overdetect_rna1_max_per_nucleus: float = 300.0` (absolute RNA1 spots/nucleus cap; <=0 disables), `qc_overdetect_robust_mad_k: float = 5.0` (run-level median+k*MAD outlier; <=0 disables) + `qc_overdetect_min_per_nucleus_for_outlier: float = 50.0` (small-signal floor for the robust test). All defaults neutral → legacy YAMLs load byte-identical; only `autofocus_channel != "dapi"` changes behaviour. 2026-07-07 MIAT×QKI ASSOCIATION METRICS (spec `_SPEC_association_analysis_2026-07-06.md`): added 2 DEFAULT-OFF `FociCfg` fields after `save_partner_rotation_null_draws` — `compute_footprint_enrichment: bool = False` (master gate for the continuous, floor-robust at-the-punctum association metrics) + `assoc_qki_floor: Optional[float] = None` (genuine QKI floor for the SECONDARY floor-gated association-ratio variant; None/<=0 = off). Both default OFF/None → byte-equivalent output; all 35 presets validate unchanged. |
| `src/fishsuite/config/presets/h9_hesc_100x.yaml` | H9 hESC 100x confocal preset (DAPI+RNA, rna_only). Validated against Brian's MIATKD_07 sweep. 2026-05-13: set `label_smoothing_radius_px: 5` to round StarDist star-convex corners that were causing sharp-angle splits in dense H9 colonies. |
| `src/fishsuite/config/presets/miat_qki_coloc_ud_g2_rna_protein_2026-06-04.yaml` | MIAT (RNA FISH, ch0/640) × QKI (IF, ch1/561) colocalization preset for the undifferentiated-hESC g2 pilot (rna_protein mode, decon-only, n=2 wDox vs n=2 noDox). Single in-focus plane (mode=autofocus, intensity-weighted + central-0.6 + z8–70 window), cellpose cpsam ds2 diam100 directml, batch-MAD pixel coloc (Pearson/Manders/Li, k=2.5) + MIAT×QKI spot pairing 0.3 µm + QKI partner-intensity at MIAT spots (single-process forced — that path hangs the parallel pool); PROVISIONAL auto_batch floors pending Sam's-method tuning. Pydantic-validated 2026-06-04. |
| `src/fishsuite/config/presets/miat_qki_coloc_ud_ALLARMS_PLAIN_strictMIAT_2026-06-05.yaml` | Full-UD MIAT×QKI coloc preset: ALL THREE dCas9-VPR arms (g2/Nog/VPR) × ±Dox = 6 conditions in ONE run, on PLAIN (non-decon) images, strict-MIAT (foci tm=1.0). 2026-06-05: switched the QKI no-spot hack (`antibody_overrides:{threshold_multiplier:50,only_nuclear_spots:true}`) to the real `foci.detect_antibody_spots: false` flag (antibody_overrides reverted to just the spot-radius entries; `compute_partner_intensity: true` kept). QKI now treated as a diffuse intensity channel — no spot carpet, coloc unaffected. Clones every analysis knob from the g2 PLAIN strictMIAT preset (autofocus iw/central-0.6/z8–70, cpsam ds2 diam100 directml, per-image MAD coloc k=2.5, MIAT×QKI 0.3 µm spot pairing + QKI partner-intensity single-process). 6 conditions assigned via `filename_conditions` (data nested arm\Dox\ and discovery is single-level, so subfolder-mode can't encode both — filename encodes both). 11 usable plain FOV (plain Nog with-Dox `_09` EXCLUDED: empty/missing .ets backing). `pub_contrast_mode: auto_per_image` (sections imaged at differing laser power → per-image-incomparable pub PNGs; analysis per-image regardless). GOTCHA: `-i` = UD root discovers 0 (single-level walk vs 2-level nesting) → MASTER CONTROL must pass a junction-staging dir of the 6 leaf folders (or run per-arm). Pydantic-validated + discovery/subset verified 2026-06-05. 2026-06-05 SCIENTIFIC-CORE: enabled the pipeline-native proper coloc null — `foci.compute_partner_null_enrichment: true` + `partner_null_n: 1000` + `partner_null_disk_px: 3.0` + `partner_null_seed: 0` + `exclude_nucleolus_from_partner_null: true`, and flipped `nucleolus.enabled: false -> true`. This makes the QKI-at-MIAT enrichment-vs-random-null + z + empirical-p (the validated `qki_at_miat_null_ALLARMS_tm1.0.py` metric) a native per_image_summary output across all 6 arms. Pydantic re-validated. 2026-06-18 SELF-SUFFICIENT COLOC OUTPUTS: flipped `foci.save_partner_null_draws: true` + `foci.compute_partner_radial_profile: true` (partner_radial_bins_um left at default [0.25,0.5,0.75,1.0]) so a NEW run writes `coloc_null_draws.csv` + `coloc_radial_profile.csv` itself and the post-run `backfill` is no longer needed going forward. Schema defaults stay OFF (BIN1/H9 unaffected). Pydantic re-validated. |
| `src/fishsuite/config/presets/miat_qki_coloc_ud_g2_PLAIN_strictMIAT_2026-06-05.yaml` | g2-only (2 noDox control + 2 wDox MIAT-OE) MIAT×QKI coloc preset on PLAIN images, strict-MIAT (foci tm=1.0), rna_protein, manual eye-tuned floors (Sam's method; MIAT 490–2250, QKI 1250–6000), per-image MAD pixel coloc k=2.5, MIAT×QKI 0.3 µm spot pairing + QKI partner-intensity (single-process). 2026-06-05: uses `foci.detect_antibody_spots: false` (replaces the threshold_multiplier:50/only_nuclear_spots hack) so QKI is treated as a diffuse intensity channel; `compute_partner_intensity: true` kept. Pydantic-validated + dry-run roster VERIFIED (4 of 8 FOV kept via input_file_subset, conditions correct). 2026-06-05 SCIENTIFIC-CORE: enabled the pipeline-native proper coloc null — `foci.compute_partner_null_enrichment: true` + `partner_null_n: 1000` + `partner_null_disk_px: 3.0` + `partner_null_seed: 0` + `exclude_nucleolus_from_partner_null: true`, and flipped `nucleolus.enabled: false -> true` (so the DAPI-poor nucleolar voids are excluded from the QKI-at-MIAT null). Pydantic re-validated. 2026-06-18 SELF-SUFFICIENT COLOC OUTPUTS: flipped `foci.save_partner_null_draws: true` + `foci.compute_partner_radial_profile: true` (partner_radial_bins_um left at default [0.25,0.5,0.75,1.0]) so a NEW run emits `coloc_null_draws.csv` + `coloc_radial_profile.csv` itself and the post-run `backfill` is unnecessary going forward. Schema defaults stay OFF (BIN1/H9 unaffected). Pydantic re-validated. |
| `src/fishsuite/config/presets/miat_qki_MAIN_singleplane_HYBRID_2026-07-04.yaml` | NEW (2026-07-04, Pydantic-validated). MIAT (561 FISH) × QKI (640 IF) coloc, H9 hESC MIAT-ASO KD vs NT, MAIN single-plane set ONLY (F:\Raw Images\Confocal Imaging\MIAT-ASO-KD; z-stacks are a separate later run). rna_protein; channel indices EXPLICIT + reversed vs ascending wavelength (ch0/640=QKI antibody, ch1/561=MIAT rna, ch2/405=DAPI); LUTs 561→magenta, 640→yellow. HYBRID QKI readout: diffuse QKI-intensity-at-MIAT null enrichment (position + rotation proper-background nulls, nucleolus-excluded, draws saved) as the headline PLUS a LOOSE QKI spot-detect (`detect_antibody_spots: true`, antibody `threshold_multiplier: 1.0`, nuclear-only, min_sep 2) for complementary MIAT×QKI spot pairing; `detect_in_sec_only: true` quantifies the non-specific QKI/MIAT spot rate in sec-only wells 7,12. mode=autofocus (every MAIN file Z=1 → sole plane used as acquired; no z-window). Well = biological replicate via `subfolder_conditions` (NT_w1/2/3/8, KD_w4/5/6/11; sec-only 7,12→Sec-Only), `min_nuclei_for_stats: 6`. cellpose cpsam ds2 diam100 directml; batch-MAD pixel coloc k=2.5; manual floors (Sam's method, all __TUNE__ starts from reality-check percentiles; QKI+DAPI floors held == the 7SK sibling for cross-preset comparability) with `apply_pub_contrast_floor_to_analysis: true`, `..._to_spots: false`; single-process (compute_partner_intensity). `input_file_subset` pins the 40 MAIN MIAT+sec-only .vsi (bare basenames) — belt-and-suspenders AND load-bearing to split MIAT from 7SK (both share the -i root; discover_inputs is a verified 2-level walk that already auto-excludes the deeper oldbad/Real z-stack folders). Run: `-i "F:\Raw Images\Confocal Imaging\MIAT-ASO-KD" -p 1` in fishproc_dml. |
| `src/fishsuite/config/presets/sk7_qki_MAIN_singleplane_HYBRID_2026-07-04.yaml` | NEW (2026-07-04, Pydantic-validated). Sibling of the MIAT preset above: 7SK (561 FISH) × QKI (640 IF) coloc, H9 hESC, MAIN single-plane set ONLY (wells 9 NT / 10 KD; 7SK = off-target high-copy-snRNA control pairing). IDENTICAL to the MIAT preset except: experiment name/description, `rna_label: "7SK-561"`, the well→condition map (NT_w9/KD_w10) + condition_order, `input_file_subset` (26 MAIN 7SK wells 9,10 + shared sec-only 7,12), and a 7SK-density note (7SK is near-confluent → the rna `threshold_multiplier` started at 1.0 likely needs to go STRICTER; NT well9 carries a saturated RNA-561 artifact to mask, so do not tune the RNA ceiling off it). Same HYBRID QKI readout (diffuse nulls headline + loose QKI spot-detect + `detect_in_sec_only`), same explicit channels/LUTs, same single-plane autofocus, same well=replicate stats, same cellpose/batch-coloc/single-process. QKI + DAPI manual floors held identical to the MIAT preset (shared partner); RNA ceiling bumped for 7SK density. Run: `-i "F:\Raw Images\Confocal Imaging\MIAT-ASO-KD" -p 1` in fishproc_dml. |
| `src/fishsuite/config/presets/*.yaml` | Other built-in presets (HEK293, U2OS, generic 60x/100x; plus the dated MIAT/QKI hESC presets e.g. `miat_oe_ud_g2_rna_only_2026-06-03.yaml`). |
| `src/fishsuite/core/io.py` | bioio-based image reader, channel autodetect, input discovery (flat or subfolder mode). 2026-06-10 BioFormats JVM-crash guard: `read_image` now stat-checks the file and raises a clean `ValueError` for any file < 512 bytes (or unstattable) BEFORE handing it to `bioio.BioImage` / the BioFormats JVM probe. A malformed/truncated/0-byte file (e.g. a stub `.vsi`) otherwise reached the JVM and triggered a hard native "Windows fatal exception: access violation" that no Python try/except could catch and that corrupted JVM state for the rest of the process (root cause of the `test_runner_input_file_subset_filter` crash — the access violation originated at `runner.py` `_io.read_image` inside the auto-batch pub-image contrast pre-pass, which is gated on `save_publication_images`, NOT on `threshold_scope`). 512 B is below every real microscopy/TIFF header but well above stubs/0-byte acquisitions, so valid images are never rejected; such files now follow the ordinary per-image-failure path (logged + excluded). 2026-05-31 filename-conditions: `discover_inputs` gained a `filename_conditions` param (ordered `[substring, condition]` pairs, default empty = legacy). For FLAT folders whose condition is encoded in the file NAME (e.g. H9 05-05 `..._NT_02.vsi` vs `..._MIAT-KD_05.vsi`), the first pair whose case-insensitive substring is found in a NON-sec file's name sets its condition; evaluated AFTER the `sec_only_*` test so sec-only files keep "Sec-Only". Wired from `ConditionsCfg.filename_conditions` via the runner. 2026-05-31 autofocus_maxproj edge-window fix: `compute_focus_window` (and `extract_dapi_focus_window`) gained two config-driven, backward-compatible (default-off) params — `intensity_weighted` (multiplies each slice's `var(laplace(plane/mean))` by its mean → `*mean`, mirroring the single-plane autofocus metric, so the focus peak follows the bright nuclear plane instead of dim/noisy stack edges) and `central_fraction` (restricts the focus-PEAK search to the middle fraction of the stack so the window anchor can never be a true edge plane). Diagnosed on H9 33-plane DAPI: plain var-of-laplacian profile is INVERTED (climbs toward dim edges; z=33 scored 100%), so it picked z=24-33/z=1-10 edge windows; intensity-weighting fixed 9/10 and central_fraction 0.6 fixed the 10th (_12). Wired from `cfg.z_stack.autofocus_intensity_weighted` + new `focus_central_fraction` at all 5 `extract_dapi_focus_window` call sites (rna_only ×2, rna_rna ×2, runner ×1). 2026-07-05 RNA-anchored autofocus + RNA signal-quality: two ADDITIVE helpers. `rna_plane_quality(plane) -> dict` returns `focus_score` (mean-normalized Laplacian variance) + `dynamic_range` = `(p99.9-median)/(1.4826*MAD)` (robust spot-callability SNR) + raw bg components; fully defensive (NaNs, never raises). `resolve_autofocus_plane(img, *, dapi_idx, rna_idx, z_start, z_end, autofocus_channel, intensity_weighted, auto_rna_quality_min) -> (abs_z_1indexed, channel_used, diag)` handles ONLY the opt-in `"rna"`/`"auto"` single-plane anchors (the LOCKED `"dapi"` default path is untouched — mode runners still call `extract_channel_autofocus_with_idx` directly for it). `"rna"` = pick sharpest RNA1 plane; `"auto"` = RNA-anchor iff the RNA-best plane's `dynamic_range >= auto_rna_quality_min` else DAPI-anchor (defensive: any RNA-autofocus failure or unknown channel falls back to DAPI). Preserves the one-plane invariant (all channels read at the single returned z). |
| `src/fishsuite/core/repro.py` | NEW (2026-06-10). ADDITIVE reproducibility + provenance helpers. `set_global_seeds(seed) -> dict` seeds Python `random`, NumPy (+ a module-level `default_rng`), `PYTHONHASHSEED`, and (if importable) `torch.manual_seed` / `torch.cuda.manual_seed_all` (guarded by `cuda.is_available()`) / `torch.use_deterministic_algorithms(True, warn_only=True)` — each backend in its OWN try/except so the function NEVER raises (even if `import torch` itself raises / torch monkeypatched absent); returns `{python_random,numpy,pythonhashseed,torch,torch_cuda,torch_deterministic}` bools. `write_versions_txt(out_dir, seed)` writes `versions.txt` (fishsuite `__version__`, python+executable, `platform.platform()`, the global seed, and per-DISTRIBUTION versions via `importlib.metadata.version` wrapped per-pkg → "not installed" on failure: numpy/scipy/scikit-image/pandas/cellpose/stardist/big-fish/torch/torch-directml/bioio/bioio-bioformats). `write_command_log(out_dir, config_path, output_dir, seed, extra=)` writes a compact `command.log` (`sys.argv`, config path, output dir, seed, + optional extra k:v like analysis_mode/z_mode). `write_run_metadata(...)` writes both. Every writer is crash-proof (returns bool / dict, never raises). Wired into `runner.run_batch`. |
| `src/fishsuite/core/qc.py` | NEW (2026-06-10). ADDITIVE per-image QC flags. `compute_qc_flags(res, cfg, dtype_max=65535) -> dict` reads a mode `ImageResult`'s `.per_image`/`.qc`/`.spots`/`.nuclei` and returns NEW informational columns the runner merges into each per-image row (mode-agnostic — computed once in the runner so rna_only/rna_rna/rna_protein/ab_ab/protein_only stay consistent without touching any mode's existing keys). Emits: `qc_frac_saturated_<role>` (role in dapi/rna/rna2/antibody, ONLY for planes present in `res.qc`; fraction of pixels >= 0.999*dtype_max), `qc_focus_score` (var-of-Laplacian of the mean-normalised DAPI plane via `scipy.ndimage.laplace`; NaN on failure), `qc_n_nuclei` (mirror of per_image n_nuclei / len(nuclei)), `qc_low_nuclei` (n_nuclei < cfg.qc.qc_min_nuclei), `qc_zero_spot` (len(spots)==0), `qc_flags` (comma-joined active flag names incl. `saturated_<role>` when frac > cfg.qc.qc_saturated_frac and `low_focus` when cfg.qc.qc_min_focus_score>0), `qc_pass` (True iff qc_flags empty). INFORMATIONAL ONLY — never drops/excludes an image. Robust to missing pieces (never raises in practice; runner also wraps the call). 2026-07-05 RNA1 OVER-DETECTION guard (ADVISORY, Brian bulletproofing): `compute_qc_flags` now also emits `qc_rna1_spots_per_nucleus` (RNA1 spots/nucleus — prefers per_image `mean_spots_per_nucleus_rna1`, else `mean_spots_per_nucleus`, else n_spots/n_nuclei; NaN when 0 nuclei) + `qc_overdetect_rna1` (bool, fires + adds `overdetect_rna1` to qc_flags when the value exceeds `cfg.qc.qc_overdetect_rna1_max_per_nucleus`, default 300; set <=0 to disable). New module-level `flag_overdetect_outliers(rows, cfg) -> int` runs ONCE per run (needs the whole batch): seeds `qc_overdetect_rna1_run_outlier=False` on every row, then flags any image whose RNA1 spots/nucleus exceeds `median + k*1.4826*MAD` (`cfg.qc.qc_overdetect_robust_mad_k`, default 5; <=0 disables) AND is above `qc_overdetect_min_per_nucleus_for_outlier` (default 50), folding `overdetect_rna1_outlier` into that row's qc_flags + qc_pass. Catches the out-of-focus-dim-RNA→BigFISH-threshold-collapse failure (well11 MIAT-KD ~1500 spots/nuc). NEVER alters detection; advisory only. Called by the runner after per_image_rows is collected. |
| `src/fishsuite/core/segmentation.py` | Nuclear segmentation wrapper around the Fiji-pipeline's `segmentation.segment_image.run_backend` (StarDist / Cellpose / Otsu). 2026-05-13: added `_smooth_label_boundaries(labels, radius)` and wired it into `segment_nuclei` via `label_smoothing_radius_px` — applies per-label morphological closing+opening with a disk SE AFTER backend postprocess (watershed/dilate/none) to round off sharp corners introduced by StarDist's star-convex polygon predictions. Default radius 0 = disabled. Anti-bleed guard: smoothed pixels are only written where the original label was 0 or k, so neighbor territory is preserved exactly. Erased-label fallback: if opening wipes a tiny/thin mask, the original pixels are restored from the input. 2026-05-29 ghost-nucleus fix: added `identify_ghost_nuclei(nuclei_df, max_dapi_cv=0.12, min_area_px=6000, ...)` — a POST-spot-detection composite classifier that returns the `nucleus_id`s of empty 'ghost' oval shells (out-of-focus border debris cellpose segments off the aberrant single-plane border band). A nucleus is a ghost only if it satisfies ALL THREE of: 0 RNA spots, large area (>=min_area_px), flat/low-texture (dapi_cv<=max_dapi_cv). Each condition alone is insufficient (on low-contrast KO images the ghost DAPI-CV band is embedded inside the real-nucleus distribution); the conjunction separates them with a safety margin. Conservative: rows missing a column or with NaN dapi_cv are kept. Pure-pandas, no image I/O. |
| `src/fishsuite/core/spots.py` | BigFISH / LoG spot detection wrapper. Returns a DataFrame of (spot_id, x_px, y_px, z_slice, intensity_peak, threshold_used). |
| `src/fishsuite/core/morphology.py` | Nuclear+cytoplasm mask helpers, spot stratification, per-nucleus regionprops. |
| `src/fishsuite/core/thresholds.py` | Threshold computation helpers. |
| `src/fishsuite/core/metrics.py` | Pixel-coloc metrics (Pearson, Manders, Li ICQ, cosine overlap). 2026-06-02: added `compute_thresholded_compartment_intensity(image_2d, mask, floor)` — threshold-and-integrate helper returning `thresh_total_intensity` (RAW sum of >=floor pixels), `thresh_mean_intensity`, `thresh_pos_area_px`, `thresh_pos_fraction` for one compartment; the core of the new thresholded-RNA-intensity feature (see `THRESHOLD_INTENSITY_FEATURE.md`). |
| `src/fishsuite/core/parallel.py` | Worker-count resolution. |
| `src/fishsuite/core/excel_report.py` | 2026-05-20 PI_Focus sheet pass: added a new `_build_pi_focus(ws, ...)` builder that emits a curated PI deliverable sheet with 5 sections (A. SPOT COUNTS PER CELL, B. SIGNAL ABOVE FLOOR, C. SPOT-INTRINSIC BRIGHTNESS, D. SPOT SIZE, E. SIGNAL LOCALIZATION) covering every metric the PI cares about — per-channel × per-compartment × condition with Mann-Whitney U p-values, Cliff's delta, and a Direction (up WT / up KO) arrow. Sections A/B/E use per-nucleus statistics (matches Comparison_Table convention); Sections C/D aggregate per-spot peak intensity + diameter to per-image means first, then test across images. Helper `_per_image_means_from_spots(spots_df, channel, compartment, value_col, real_only_mask)` filters per-spot rows by `channel`/`in_nucleus`/`in_cytoplasm` and groups by (image, condition). New section-fill palette `PI_FOCUS_CATEGORY_FILLS` colors each metric-name cell by category. The new sheet is wired into `write_analysis_summary_workbook` as the 3rd sheet (between Executive_Summary and Comparison_Table) — sheet count grows from 9 to 10. README "B. SHEET INDEX" gains a PI_Focus row; README "E. INTERPRETATION GUIDE" gains an "Above-floor intensity" explainer (= sum of `max(pixel - floor, 0)` over the masked region). Post-build label substitution now includes PI_Focus so "RNA1"/"RNA2"/"DAPI" tokens remap to preset labels (Introns/Exons/DAPI for the BIN1 preset). Generic Excel-report module documented here for the first time — pre-existing builders (`_build_readme`, `_build_executive_summary`, `_build_comparison_table`, `_build_run_config_sheet`, `write_analysis_summary_workbook`, `write_raw_data_workbook`) unchanged in behavior. |
| `src/fishsuite/core/output.py` | 2026-06-05 honest rna_rna/rna_protein threshold-panel fix (Brian: "the threshold images are definitely messed up badly"): `save_walkthrough_bundle_rna_rna` gained `floor_is_spot_gate: bool = True`. The step05/06 panels threshold each channel at `rna*_floor_override` (the pub DISPLAY floor). That is honest ONLY when the display floor IS the spot-detection gate (`apply_pub_contrast_floor_to_spots: true`, e.g. BIN1 rna_rna/rna_protein presets) — default `True` keeps that path byte-identical. When the flag is `False` (MIAT-QKI: MIAT display floor set LOW 490–525 to show faint cytoplasmic MIAT, and `apply_pub_contrast_floor_to_spots: false` so spots come from BigFISH LoG, NOT the floor), thresholding the channel at the cosmetic floor floods each nucleus into a solid yellow mass unrelated to the ~66 discrete called puncta. Fix: for the `False` branch, a channel WITH detected spots renders the ACTUAL called spots (`draw_spot_markers` on black for step05, on the gray channel for step06) titled "<chan> detected spots (BigFISH LoG)"; a channel WITHOUT spots (diffuse QKI antibody under `detect_antibody_spots: false` → empty `spots2`) keeps the floor-fill (representative for a diffuse intensity channel — no crash, no empty overlay). Filenames (`__step05_<suffix>_threshold`, `__step06_<suffix>_threshold_on_signal`) UNCHANGED in both branches, so downstream QC scripts + the locked test are unaffected. Runner passes `floor_is_spot_gate=bool(cfg.output.apply_pub_contrast_floor_to_spots)`. Spot detection, coloc, and every quantitative output untouched (display-only). New test `test_rna_rna_walkthrough_floor_not_spot_gate_shows_detection` locks it; test_output.py 17/17, full suite 83 pass. 2026-06-02 walkthrough spot-floor threshold fix: `save_walkthrough_bundle` (rna_only) gained an optional `spot_floor: Optional[float] = None` param. When supplied (runner passes `output.manual_rna_min` whenever `apply_pub_contrast_floor_to_spots` is True and the floor > 0), the step05/06/08 "threshold" panels now render the EFFECTIVE SPOT GATE (pixels >= spot_floor) instead of the pixel-coloc MAD mask in `rna_pos_mask` — the MAD threshold is the internal coloc cut (e.g. ~290), much lower than the 800 spot floor, so the old panels lit up most of the field and looked wildly over-exposed, misrepresenting the actual gate Brian flagged. Step06's gray base floor is anchored to `spot_floor*0.75` (was `rf*0.75`) and all three panels get a burned top-left title via the new `burn_corner_title(rgb, text, ...)` helper (e.g. "MIAT-640 ≥ spot floor 800"). When `spot_floor` is None the panels fall back to the legacy `rna_pos_mask` behaviour (backward compatible). DISPLAY-ONLY: spot detection, the floor-filter, pixel_coloc, and every quantitative output are untouched. Test suite 88 pass (test_output.py 16/16). 2026-05-29 step09 filename fix: the rna_rna walkthrough's step09 panel now writes `<stem>__step09_active_TSS.png` (was the stale `__step09_nuclear_overlap_spots.png`), matching the feature's own comment ("Step 09: Active TSS"), the `n_active_tss_per_nucleus` mode terminology, and the locked test `test_save_walkthrough_bundle_rna_rna_emits_extended_steps`. QC-only PNG rename — no change to any quantitative deliverable or validated image path. 2026-05-18 pub-contrast overrides pass: `save_publication_images_bundle` gained per-channel absolute `*_floor` / `*_ceil` `Optional[float]` kwargs (`dapi_floor`/`dapi_ceil`, `rna_floor`/`rna_ceil`, `rna2_floor`/`rna2_ceil`, `ab_floor`/`ab_ceil`) that, when both halves are supplied, bypass percentile + batch-cache work entirely and use the values verbatim. When only one half is supplied the other half falls through to the legacy percentile+cache path so callers can pin just a floor or just a ceiling. Internal `_resolve` helper centralizes the three-tier priority (absolute override → batch cache → per-image percentile) for all four channels. New `save_tifs: bool = True` kwarg gates the .tif emission alongside the .png — `_save_dual` only writes the .tif when True; default preserves historical PNG+TIF dual-save for callers that don't pass it. Wired from `OutputCfg.save_publication_tifs`. 2026-05-15 walkthrough audit pass (Brian "i seem to have lost some of the actual steps within the qc overlays and the pipeline walkthrough"): expanded both walkthrough bundles. `save_walkthrough_bundle` (rna_only) now optionally accepts `spots` + `cyt_labels`; emits step07_spots_on_DAPI, step08_spots_on_RNA_threshold, step09_cytoplasm_mask in addition to the existing step01-06. `save_walkthrough_bundle_rna_rna` extended from 9 to 15 panels: step07_RNA1/2_spots_on_DAPI (per-channel spots on DAPI), step08_paired_spots (DAPI+RNA1+RNA2 composite with paired-spot green rings — uses the dynamic `paired_at_<X>um` column; falls back gracefully when missing), step09_active_TSS (nuclear AND paired RNA1 spots highlighted — Brian's active-transcription proxy), step10_cytoplasm_mask (cytoplasm outlines green + nucleus outlines white), step11_merge_all (DAPI+RNA1+RNA2+nucleus outlines composite). `render_all_in_one_qc_rna_rna` + `save_walkthrough_bundle_rna_rna` now default to MAGENTA for RNA2 (Brian's 2026-05-14 RNA-FISH wavelength→color convention: 561→magenta, 647→yellow); cyan still selectable via `rna2_color` + `rna2_lut_weights` kwargs for legacy datasets. 2026-05-13 BIN1 Run1 issue 4 (color selection): added named-LUT lookup map `_LUT_NAME_TO_RGB_WEIGHTS` (blue/yellow/cyan/magenta/green/red/orange/gray/fire) + `lut_name_to_weights(name, fallback)` helper. `save_publication_images_bundle` gains four new optional kwargs `dapi_lut` / `rna_lut` / `rna2_lut` / `antibody_lut` that control the pseudo-color used to tint each channel; the suffix in every emitted filename uses the chosen LUT name (e.g. `__RNA_red.png` when `rna_lut="red"`). Defaults match the historical Blue/Yellow/Cyan/Magenta so configs without these knobs render byte-identically. 2026-05-13 channel-labels pass: added `sanitize_label_for_filename(label, default)` mirroring the condition-sanitizer for safe filename embedding; added `burn_channel_legend(rgb, entries)` that paints a stacked "<swatch> <label>" legend into the top-left corner of QC overlays with a dimmed backdrop for readability; `render_all_in_one_qc` / `render_all_in_one_qc_rna_rna` now accept `dapi_label` / `rna_label` / `rna2_label` kwargs and burn the legend automatically; `save_publication_images_bundle` now accepts `dapi_label` / `rna_label` / `rna2_label` / `antibody_label` and uses them in every emitted filename (e.g. `<stem>__MIAT_Cy5_yellow.png`, `<stem>__merge_DAPI_MIAT_Cy5.png`). Defaults match the historical role names so back-compat is preserved. Pure-Python rendering + writers for Fiji-compatible outputs: blue/yellow/magenta/cyan LUTs, additive RGB merge, scale-bar burn (50um, height=12, font=28), QC overlays, walkthrough steps, popouts, mask TIFFs. 2026-05-11 Fiji parity pass: added batch-coordinated running-max contrast cache (matches Fiji's _BATCH_DISP_CEIL_CACHE), added Protein_magenta + multi-channel merge support (DAPI+Prot, RNA+Prot, all-three), DAPI uses per-image p10/p99.9 while RNA/AB use batch_key-coordinated p95/p99.95 (RNA) / p80/p99.5 (AB). 2026-05-12 output-fixes pass: (a) `sec_only` kwarg added to `_resolve_lut_range` / `render_all_in_one_qc` / `save_publication_images_bundle` / `save_walkthrough_bundle` — sec-only images consult-but-do-not-update the batch contrast cache (Fiji parity with `compute_pub_images_batch_contrast` lines 3990-3992), so no-probe controls render at the same contrast scale as real images and look correctly dim; (b) `burn_scale_bar` upgraded — bold-font preference chain (`arialbd.ttf` first), micro-sign label ("50 µm"), 2 px black stroke outline via PIL `stroke_width`/`stroke_fill` for crisp readability over bright yellow regions; (c) new `sanitize_condition_for_filename` helper used by the runner to embed condition labels into per-image output filenames. 2026-05-13 rna_rna pass: (a) `save_publication_images_bundle` accepts an `rna2` ndarray and emits `<stem>__RNA2_cyan` (cyan 0/1/1 LUT, batch_key="rna2") + `merge_DAPI_RNA1` + `merge_DAPI_RNA2` + `merge_DAPI_RNA1_RNA2`; (b) new `render_all_in_one_qc_rna_rna(dapi, rna1, rna2, labels, spots1, spots2, ...)` QC overlay (yellow markers for RNA1 spots, cyan for RNA2); (c) new `save_walkthrough_bundle_rna_rna(...)` emits step04/05/06 for BOTH channels (`step05_RNA1_threshold` + `step05_RNA2_threshold`). |
| `src/fishsuite/core/modes/__init__.py` | Mode registry. 2026-07-03: also imports/registers `if_intensity`. |
| `src/fishsuite/core/modes/rna_only.py` | rna_only analysis mode. Emits Fiji-schema per-nucleus, per-spot, cell-morphology, per-image-summary, and thresholds dicts so combine_to_xlsx.py + R scripts can consume the output transparently. 2026-05-11 Fiji parity fix: replaced BigFISH/Otsu-derived `rna_thr_value` with Fiji's pixel-coloc MAD threshold (median + k_mad * MAD over RAW nuclear pixels, NO preprocessing — confirmed against Coloc_Pipeline.py lines 682/752/760-761). H9 preset uses per_image scope + k_mad=2.5. 2026-05-12 batch-scope fix: `run_one` now accepts an optional `precomputed_rna_threshold` kwarg used verbatim when supplied (set by the runner's batch pre-pass); new `collect_nuclear_rna_pixels()` helper extracts raw nuclear RNA pixels for one image so the runner can pool across the batch. 2026-05-29 ghost-nucleus fix: after the nucleolus/chromatin-metrics merge (and before the per-image summary), an OPT-IN block gated on `cfg.nuclei.reject_ghost_nuclei` (default OFF) calls `_seg.identify_ghost_nuclei` to drop empty 'ghost' shells. When a nucleus is flagged it is removed from `nuclei_df`, its label is zeroed in `labels` (so saved masks / QC overlays / popouts / nuc_mask / counts stay consistent), and its spots are dropped from `spots_out_df`; the per-image drop count is logged. If `dapi_cv` is absent (nucleolus disabled) it is computed on the fly from `chromatin_metrics_per_nucleus`. 2026-06-02 thresholded-RNA-intensity feature: new module-level helper `_resolve_thresh_intensity_floor(cfg, analysis_floors, channel)` (precedence: `output.<ch>_intensity_threshold` pin -> runner-forwarded `analysis_floors[ch]` spot floor -> `manual_<ch>_min` -> NaN), and the per-nucleus loop now emits `rna_thresh_total_intensity_nuclear`/`_mean_intensity_nuclear`/`_pos_area_px_nuclear`/`_pos_fraction_nuclear` + `_cyto` equivalents + `rna_thresh_floor`, with per-image `mean_rna_thresh_*` rollups in the summary. Floor applied to the spot-detection RNA plane (`rna_2d`). Distinct from spot intensities and from raw `sum_rna_intensity`. |
| `src/fishsuite/core/modes/rna_protein.py` | 2026-06-19 ROTATION 'proper background' null (native port of the adversarially-validated rotation_null_prototype.py): added `coloc_rotation_null` to the extra-DataFrame rna2->protein column-relabel loop (so the rotation extra surfaces under protein-*). rna_protein mode (rna_only + per-nucleus pixel coloc). 2026-06-05 SCIENTIFIC-CORE: inherits the per-nucleus random-position NULL coloc via the shared rna_rna core + the rna2->protein relabel (no rna_protein code change — the deep-copy shim carries the 5 new foci flags verbatim). Per-nucleus `rna2_enrichment_vs_null_at_rna1_spots`/`rna2_null_z_at_rna1_spots` read `protein_enrichment_vs_null_at_rna1_spots`/`protein_null_z_at_rna1_spots`; per-image `rna2_pooled_*` -> `protein_pooled_*`. This is the headline MIAT x QKI metric (QKI=antibody->rna2/protein slot). 2026-06-05 detect_antibody_spots flag: `run_one` now passes `rna2_is_antibody=True` into `rna_rna.run_one` so the antibody(→rna2) channel honors `cfg.foci.detect_antibody_spots` — when False, rna_rna skips antibody spot detection (diffuse QKI carpet fix) while the QKI-pixel-intensity-at-MIAT-spots coloc + pixel-coloc still run. `_build_rna2_shim_cfg` deep-copies the flag verbatim onto the shim; only rna_protein sets `rna2_is_antibody=True`, so plain rna_rna is never affected. 2026-06-02: inherits the thresholded-RNA-intensity feature via rna_rna; the antibody channel (rna2 slot) gets the same threshold-integrate logic, columns relabeled `rna2_thresh_*`->`protein_thresh_*` (and `rna2_thresh_floor`->`protein_thresh_floor`) by the standard rna2->protein relabeling. |
| `src/fishsuite/core/modes/rna_rna.py` | 2026-06-19 ROTATION 'proper background' null (native port of the adversarially-validated rotation_null_prototype.py): new SHARED helpers `_rotate_pts`, `_rotation_null_for_nucleus` (keep-N redraw about the CONSTELLATION centroid; returns obs+null_stats+null_stats_drop+median_retention+usable), `_translation_null_for_nucleus`, `_rotation_single_position_dist`; run_one wires a rotation branch into the existing per-nucleus partner block (reuses _samp_mask/_scy/_scx), emitting per-nucleus `rna2_rotation_enrichment/null_z/null_p/assoc_fraction_at_rna1_spots` + `rotation_null_usable`/`rotation_median_retention`, a spot-weighted per-image pooled rollup `rna2_pooled_rotation_*` (empirical p from the full-length keep-N pool over rot-usable nuclei) + optional `extra['coloc_rotation_null']`; optional translation companion (pools per-nucleus enrichment). All gated on `compute_partner_rotation_null` (default OFF -> byte-identical). rna_rna analysis mode — two RNA channels with independent BigFISH spot detection. 2026-06-05 SCIENTIFIC-CORE (pipeline-native proper coloc statistic): ports the validated external script `qki_at_miat_null_ALLARMS_tm1.0.py` into the pipeline. New module helpers (L177-305): `_disk_stencil(radius_px)` (in-disk integer offset stencil, r=3 -> 29-px disk), `_disk_means_at(partner_2d, cys, cxs, dy, dx)` (numpy-batched per-center disk-mean, clip-to-edge), `_partner_null_for_nucleus(...)` (per-nucleus observed stat + the (n_null,) null distribution; draws `n_null*n_sp` random in-nucleus pixel indices in ONE batch then reshapes -> 1000 draws x hundreds of nuclei in <1 s, vs the reference's per-spot Python loop). Per-nucleus loop preamble (L990-1062, gated on `compute_partner_null = compute_partner_intensity AND foci.compute_partner_null_enrichment`): builds the RNG (fixed `partner_null_seed`), the disk stencil (`partner_null_disk_px`, default 3.0), spot-count-weighted pooled accumulators, and — when `exclude_nucleolus_from_partner_null` AND `nucleolus.enabled` — detects nucleoli ONCE via `core.nucleolus.detect_nucleoli` (REUSED by the post-loop nucleolus column block at L1812 so detection runs once). In the loop (L1352): observed = mean over THIS nucleus's rna1 (MIAT) spots of [disk-mean rna2/QKI intensity]; null = same #spots at random IN-NUCLEUS positions (nucleus minus nucleoli when excluding) x `partner_null_n`; emits per-nucleus `rna2_enrichment_vs_null_at_rna1_spots` + `rna2_null_z_at_rna1_spots` (gated injection; rna_protein relabels rna2->protein); drops observed rna1 spots inside a nucleolus when excluding. Per-image pooled rollup (L1623): spot-count-weighted pooled enrichment / z / empirical-p -> per_image `rna2_pooled_enrichment_vs_null_at_rna1_spots`, `rna2_pooled_null_z_at_rna1_spots`, `rna2_pooled_null_p_empirical_at_rna1_spots`, `rna2_pooled_obs_at_rna1_spots`, `rna2_pooled_null_mean_at_rna1_spots`, `n_nuclei_partner_null`, `partner_null_n`, `partner_null_disk_px` (gated, both populated + empty-image branches at L2143). Canonical params chosen: DISK_R=3.0 px, N_NULL=1000, in_nucleus spots, spot-count-weighted pooling, seed=0. RECONCILIATION of the two reference scripts: they differ ONLY by nucleolus exclusion (both use DISK_R=3) — the with-nucleoli vs nucleolus-excluded per-FOV enrichments are ~1.15-1.21 vs ~1.13-1.18 (~0.01 lower; the "1.07" in the task brief was an earlier g2 pilot at a different radius, not the trusted scripts). Native helper VERIFIED against the validated JSON on real masks+spot_metrics+raw VSI (NO GPU run): g2_wDox_01 native 1.2095 vs script 1.2094, g2_noDox_03 1.1801 vs 1.1801, VPR_noDox_14 1.1666 vs 1.1659 (enrichment diff <=0.0007; tiny residual from vectorized batched-RNG draw order vs the script's per-spot loop, affects null SD/z by ~1.5% but not the enrichment). Defaults OFF -> byte-identical (test-proven). 2026-06-05 detect_antibody_spots: `run_one` gained `rna2_is_antibody: bool = False` (default False = plain rna_rna byte-identical). When True (set ONLY by rna_protein.run_one) AND `cfg.foci.detect_antibody_spots` is False, the rna2 `_detect` call is replaced with an EMPTY frame (`pd.DataFrame(), nan`) — the same representation `_detect` returns when foci are disabled, which the QC overlay (`render_all_in_one_qc_rna_rna`), walkthrough (step07b/08/09 self-skip on empty spots2; step05b/06b are pixel-mask panels, unaffected), pairing (`_nn_distances`/`_spot_coords_um` → inf/empty), and the per-nucleus rollups (`_agg`/`_active_ts_count`/`_partner_local_mean_over_spots` all guard empty) already tolerate. The rna2 PIXEL plane is still loaded, so pixel-coloc + partner-intensity (samples rna2 pixels at rna1 spots) are unchanged. Verified end-to-end on a synthetic stack (detect=True→54 protein spots; detect=False→0 protein spots, 49 MIAT spots, partner-intensity coloc populated; plain rna_rna flag=False→54 rna2 spots = flag ignored). independent MAD-based pixel-coloc thresholds (per-channel batch pre-pass), and between-channel spot-to-spot colocalization via scipy.spatial.cKDTree (per-spot `nn_distance_um`, configurable `paired_at_<X>um`, per-nucleus + per-image aggregates `paired_fraction_rnaN_at_<X>um` + `median_nn_distance_rnaN_um`). 2026-05-14 exon/intron biology pass: per-nucleus rows now emit `nuclear_total_intensity_rna1`/`_rna2`, `cytoplasmic_total_intensity_rna1`/`_rna2`, `nc_ratio_total_intensity_rna1`/`_rna2` (total-intensity N/C ratio, not just the mean-based one), `n_active_tss_per_nucleus` (paired AND nuclear RNA1 spots — exon/intron co-localized punctae at the gene locus), `n_active_tss_per_nucleus_rna2` (same for RNA2 — symmetric), and `n_mature_mrna_rna1_per_cell` / `_rna2_per_cell` (cytoplasmic spot count = exported mRNA pool proxy). The per-image summary mirrors these as `mean/median_nc_ratio_total_intensity_*`, `mean/median_n_active_tss_per_nucleus`, `mean/median_n_mature_mrna_*_per_cell`. Fully back-compat — new columns appear in addition to the existing rna_only-compatible schema. 2026-05-13 per-channel-overrides pass: the per-image worker now resolves spot-detection params via `cfg.foci.resolved_for('rna')` / `resolved_for('rna2')` before each BigFISH call, so the two channels can use different `bigfish_spot_radius_nm` / `bigfish_spot_radius_z_nm` / `threshold_multiplier` / `only_nuclear_spots` / `min_sep_px` values. Validated on H9 MIATKD_07: with shared tm=0.5 + rna2_overrides.threshold_multiplier=0.7, RNA1 unchanged (1719 spots, thr=22) while RNA2 dropped from 1744 to 373 (thr 21 → 29). Thresholds dict gains 10 new columns (`rna_/rna2_bigfish_spot_radius_nm`, `..._z_nm`, `..._threshold_multiplier`, `..._only_nuclear_spots`, `..._min_sep_px`) so both channels' effective params are recorded for audit. Exposes `run_one` (per-image worker, accepts `precomputed_rna_threshold`/`precomputed_rna2_threshold`), `collect_nuclear_rna_pixels` (returns the tuple `(rna_pixels, rna2_pixels)` for the runner's pre-scan), and `_resolve_channels` (validates rna2 distinct from rna). Per-spot CSV uses a single DataFrame with a `channel` column ('rna1'/'rna2') plus `in_nucleus`/`in_cytoplasm` int-flags; the runner additionally writes `<stem>__spot_metrics_rna1.csv` + `<stem>__spot_metrics_rna2.csv` for convenience. 2026-05-13 expansion pass: (a) per-nucleus rows gain `cell_total_intensity_rna1`/`_rna2` (raw pixel sum over nucleus+voronoi cytoplasm = the per-cell total intensity Brian wants for both channels), `cell_area_px`, `sum_rna_intensity_cyto`/`sum_rna2_intensity_cyto`; (b) per-image-summary gains the full rna_only intensity-stat block mirrored for both channels: `mean/median/cv_cell_total_peak_intensity_rna1`/`_rna2`, `mean/median_nuc_total_intensity_rna1`/`_rna2`, `mean/median/cv_cell_total_spot_peak_intensity_rna1`/`_rna2`, plus spots-per-nucleus `cv_*_rna1`/`_rna2`, `frac_nuclei_with_ge_{1,5,10}_spot(s)_rna1`/`_rna2`, nuclear-vs-cyto rollups `nuclear_spots_rna1`/`cytoplasmic_spots_rna1` + `_rna2` and `frac_nuclear_rna1`/`_rna2`, and threshold provenance `rna_threshold_value`/`rna2_threshold_value`/`rna_bigfish_log_threshold`/`rna2_bigfish_log_threshold`; (c) thresholds row gains a full RNA2-mirrored block (`rna2_threshold_mode`, `rna2_threshold_k_mad`, `rna2_threshold_scope`, `rna2_bigfish_log_threshold`) so both channels' threshold provenance is fully captured. **2026-05-29 BUGFIX (intensity-coloc per-nucleus all-NaN):** with `foci.compute_partner_intensity=True`, the per-nucleus columns `rna2_local_mean_at_rna1_spots` / `rna2_enrichment_at_rna1_spots` / `rna1_local_mean_at_rna2_spots` / `rna1_enrichment_at_rna2_spots` (relabeled in rna_protein to `protein_local_mean_at_rna1_spots` etc.) came out ALL NaN despite a fully-populated per-spot `partner_local_mean_intensity`. Root cause: the per-nucleus spot index `spots1_by_nid`/`spots2_by_nid` was built via `groupby` BEFORE the `partner_local_mean_intensity` column was added to the parent spot frame, so the grouped sub-frames never saw the column and the aggregation returned NaN for every nucleus (the per-spot CSV, written from the parent frame, was unaffected). Fix: moved the two `_by_nid(...)` calls to AFTER all per-spot column construction (nn_distance/paired, spot_diameter, partner intensity), just above the per-nucleus loop. 2026-06-02 thresholded-RNA-intensity feature: imports `_resolve_thresh_intensity_floor` from rna_only, resolves a floor for EACH channel (rna + rna2/antibody), and the per-nucleus loop emits the `rna_thresh_*` AND `rna2_thresh_*` column sets (nucleus + cytoplasm) + `rna_thresh_floor`/`rna2_thresh_floor`, with per-image `mean_*` rollups via the existing `_img_stats` helper. rna2 floor sources are rna2-specific (`rna2_intensity_threshold`/`manual_rna2_min`/`analysis_floors['rna2']`). 2026-06-06 NATIVE coloc-figure outputs (`E:\Claude\fishsuite\src\fishsuite\core\modes\rna_rna.py`): added two MODULE-LEVEL importable helpers — `_annulus_stencils(bins_px) -> list` (concentric-ring (dy,dx) offset masks: ring 0 = inner disk incl. center `0<=d<=bins[0]`, ring i = annulus `bins[i-1]<d<=bins[i]`; mirrors `_disk_stencil`) and `_radial_profile_for_nucleus(partner_2d, spot_cys, spot_cxs, nuc_ys, nuc_xs, stencils, n_null, rng) -> list of per-ring (obs_mean, null_mean, null_sd, n_spots)` (per-nucleus radial QKI-around-MIAT profile sharing ONE random reference draw across rings; a backfill module imports both). In `run_one`: when `foci.save_partner_null_draws` is on the already-computed pooled null vector + pooled observed are surfaced via `extra["coloc_null_draws"]` (cols `image,condition,iter,pooled_null_value,pooled_obs`; NO recompute); when `foci.compute_partner_radial_profile` is on the radial loop runs in the SAME per-nucleus block as the disk null (shared nucleolus-excluded `_samp_mask`, same seed/n via a SEPARATE rng so the disk-null pooled values stay byte-identical) and emits `extra["coloc_radial_profile"]` (cols `image,condition,ring_um,obs_mean,null_mean,null_sd,enrichment,z,n_spots`). Both gated, default OFF → keys absent → byte-identical BIN1/H9. `rna_protein.run_one` routes both DataFrames' columns through `_relabel_df_columns` (rna2→protein); `runner.run_batch` concats across images → `coloc_null_draws.csv` / `coloc_radial_profile.csv` (no file when absent). 2026-07-05 RNA-ANCHORED AUTOFOCUS (opt-in): the single-plane `z_mode == "autofocus"` branch in `run_one` now reads `cfg.z_stack.autofocus_channel`. When `"dapi"` (DEFAULT) the ORIGINAL DAPI-anchor code runs UNCHANGED (byte-identical). When `"rna"`/`"auto"` it calls `io.resolve_autofocus_plane` to pick ONE plane anchored on the RNA1 channel (auto = gated on the RNA `dynamic_range` vs `autofocus_auto_rna_quality_min`), then reads DAPI (seg) + RNA + antibody all at that same z via `extract_channel_at_z` (one-plane coloc invariant preserved). ONLY in the `"rna"`/`"auto"` case does it add GATED audit columns to per_image via a `_z_af_extra` dict merged just before `return ImageResult` (covers BOTH the n>0 and no-nuclei paths): `z_autofocus_mode`, `z_autofocus_channel_used`, `z_plane` (1-indexed), `rna_focus_score`, `rna_dynamic_range`, `rna_n_confident_spots` (len rna1 spots), plus `z_autofocus_rna_quality_score`/`_min` in auto mode. Default `"dapi"` leaves `_z_af_extra` empty → per_image_summary byte-identical. NOTE: `collect_nuclear_rna_pixels` (batch pub-contrast pre-scan) intentionally still uses the DAPI plane — display-only pooling, not analysis, kept unchanged for minimality. 2026-07-07 MIAT×QKI ASSOCIATION METRICS (spec `_SPEC_association_analysis_2026-07-06.md`; gated on `foci.compute_footprint_enrichment`, default OFF → byte-identical): new module helpers `_footprint_disk_fallback(...)` + `_sample_qki_at_miat_footprint(rna1_2d, partner_2d, spots_df, voxel_xy_um, *, default_spot_diameter_um, ...) -> (fp_qki, fp_area, union_mask)` — for EACH rna1 (MIAT) spot, sample the partner (rna2/QKI) intensity over the spot's EXACT half-max (FWHM) footprint (window sized from the per-spot MEASURED `spot_diameter_um`; seed = brightest MIAT pixel within ±1px of centroid; local bg = 10th-pct of window; threshold = `bg + 0.5*(peak-bg)`; footprint = the 8-connected component ≥ threshold CONTAINING the seed → SIZE-SCALING, not a fixed disk), with a per-spot fitted-radius disk fallback (radius=FWHM/2) when the half-max footprint is impractical (edge/flat/seed-below-thr). `run_one` wiring (just before the `_by_nid` index build): builds a per-nucleus QKI-mean map via `scipy.ndimage.mean` over `labels` (== the loop's `rna2_mean` denominator, so per-spot enrichment and the per-nucleus ratio are internally exact), writes per-spot `qki_at_miat_footprint`/`miat_footprint_area_px`/`qki_footprint_enrichment` (= footprint QKI ÷ that spot's-nucleus mean QKI) onto spots1_df + a footprint UNION mask. In the per-nucleus loop it emits `qki_assoc_ratio_continuous` (mean of per-spot enrichment over the nucleus's MIAT spots; PRIMARY), an optional SECONDARY floor-gated `qki_assoc_ratio_gated_<floor>` (spots with footprint QKI < `foci.assoc_qki_floor` contribute 0; gated ≤ continuous), threshold-free `coloc_moc` (= `_cm['cosine_overlap']`, Manders Overlap Coefficient R) + `coloc_icq` (= `_cm['li_icq']`, Li's ICQ) surfaced ALONGSIDE the MAD-thresholded Manders M1/M2, and `qki_at_miat_foci_enrichment` (mean QKI over the union of the nucleus's footprint pixels ÷ nuclear-mean QKI). Per-image `_img_stats` rollups `mean_qki_assoc_ratio_continuous`/`mean_coloc_moc`/`mean_coloc_icq`/`mean_qki_at_miat_foci_enrichment` (+ gated) in BOTH the populated + empty-image branches. Column names are FIXED literals (not rna1/rna2 relabeled, so `_relabel_rna2_to_protein` leaves them intact). The footprint per-spot loop runs in-process (like `compute_partner_intensity`; use -p 1). All gated OFF by default → byte-identical (test-proven, 14 new tests in test_footprint_association.py). |
| `src/fishsuite/core/modes/{ab_ab,protein_only,pub_images}.py` | Phase-2 stubs that currently re-use rna_only. (`protein_only` remains a stub; the real IF-intensity capability lives in `if_intensity.py`.) |
| `src/fishsuite/core/modes/if_intensity.py` | NEW (2026-07-03). First-class IMMUNOFLUORESCENCE antibody-validation intensity mode (`analysis_mode: if_intensity`) — a PLATE-level pipeline diverted from `runner.run_batch` (single guard; FISH modes untouched) into `run_if_batch`. Faithful port of the locked panQKI WT-vs-QKI-KO standalone (`F:\Image Analysis Work\MIAT-QKI-Coloc\WT-QKI-KO_2026_07_01\_scripts\`): forced `bioio_bioformats` loader + scene-picker + `get_image_data('CYX',T=0,Z=0)`; reuses `core.segmentation.segment_nuclei` (cpsam/DirectML, the SAME 8-key params → identical masks/numbers) + `exclude_border_labels`; per-secondary channel routing (647→"640"ch, 568/"565"→"561"ch, DAPI→"405"; substring, one-hit-or-raise); per-nucleus nuclear mean+integrated (RAW, regionprops), whole-FOV total + total/DAPI, cytoplasmic dilated-ring (`expand_labels`, `cyto_ring_px`); WARN-not-raise exposure-identity gate (within each routed channel, tol 1e-6); `pool_seconly` + per-well canonical fold-over-secondary-only (genotype+secondary matched); FOV→well aggregation (replicate unit = WELL; 3 FOV averaged in); Welch primary + Student + Cohen's d with honest n<2; three stat blocks (`per_secondary_raw`/`primary_vs_seconly`/`pooled_fold_n3`); comparison groups AUTO-derived from the plate map (no hardcoded wells). Writes the standard master CSVs (`per_image_summary`, `nuclei_metrics`) + IF deliverables (`per_nucleus`/`per_fov`/`per_well`/`stats`/`exposure_report`/`seconly_pooled`/conflict-well-check) + `masks/*.npy`; figures/workbook via `if_report`. Reproduces the standalone 647-arm `nuc_mean_qki` WT≈16648 vs KO≈3923, Welch p≈0.0146. Config = `IfIntensityCfg` + the shared `nuclei` block. Registered but its per-image `run()` raises (plate-level only). 2026-07-05 NATIVE PUB IMAGES: `run_if_batch` now (after micrographs) calls `if_pub_images.write_run_context(...)` (writes `if_run_context.json` for GPU-free regeneration) and, when `if_intensity.pub_images` (default ON), `if_pub_images.make_pub_images_live(output_dir, plate, per_fov, input_dir, cfg, px_um)` to render the native `publication_images/` set (both sources, all wells); wrapped in try/except so a failure never aborts the run. |
| `src/fishsuite/core/modes/if_report.py` | NEW (2026-07-03). Figures + Excel workbook for the `if_intensity` mode. SuperPlots (locked Prism/Okabe-Ito: WT `#E69F00`, KO `#009E73`, sec-only `#999999`; bar of per-well means +SD only when n≥2 over a per-nucleus cloud tinted by well + per-well mean dots; per-secondary 2-group nuc_mean+ratio, 3-group floor, pooled-fold n3; 600 DPI, no red+green). SHARED-display-range micrographs ("Sam's floor": ONE vmin/vmax per secondary — vmax = WT-primary `display_ceiling_pct`(99.5), vmin = secondary-only `display_floor_pct`(50/median); applied identically to WT/KO/sec-only, NEVER per-image auto-contrast; LUT by wavelength 647→yellow / 568→magenta / DAPI→blue; central in-focus WINDOWED max-projection when a z-stack is present (Brian's improvement over full-stack max); `_display_ranges.txt` provenance) + the WT|KO|secondary-only MONTAGE (money antibody-validation panel). Optional explorable `.xlsx` (How_to_read sheet FIRST; wrapped so a failure never aborts the run). |
| `src/fishsuite/core/modes/if_pub_images.py` | NEW (2026-07-05). NATIVE publication-image renderer for `if_intensity` — faithful fold-in of the locked panQKI standalone `pub_images.py` (`F:\Image Analysis Work\MIAT-QKI-Coloc\WT-QKI-KO_2026_07_01\_scripts\pub_images.py`) so the antibody-validation pub images are produced BY the mode (and regenerable from a finished run WITHOUT the GPU) instead of a one-off script. CPU only (bioio_bioformats read; NO cellpose/DirectML). Renders, per representative well per secondary, from TWO SOURCES: `single_plane` (representative FOV of the quantification set = densest FOV per well from per_fov counts, middle fallback) and `picked_z` (the SINGLE best-focus z-plane — `var(laplace)*mean` on DAPI, central `pub_z_central_frac`=0.8 band, **NO MIP**). Per well: a signal\|DAPI\|Merge channel panel (each panel its own scalebar) + a standalone Merge; plus one WT/KO/secondary-only COMPOSITE per secondary/source. REUSES `if_report._lut`/`_norm`/`_focus_scores` (no duplication) + `if_intensity._discover_wells`/`_install_asarray_shim`; DAPI_WEIGHT=0.6 in merge only. Display = RAISED per-secondary floor (`pub_display_floors` default 647=5000/568=3500 → WT cytoplasm near-zero) with ceiling = `pub_ceiling_pct`(99.5) of the pooled WT-primary signal, matched per secondary+source, applied identically to WT/KO/sec-only. CRITICAL channel rule enforced from each well's `qki_channel`: 647→640+DAPI only, 568/565→561+DAPI only (never the empty channel); logged per read to `channel_rule_log.txt`. Renders ALL wells (not one rep). Outputs → `<run>/publication_images/{single_plane,picked_z}/*_channels.png` + `*_merge.png`, `composite_<sec>_<source>.png`, `versions.txt`/`channel_rule_log.txt`/`qc_nuclear_dominance.txt`. `build_pub_images(...)` = config-agnostic core; `make_pub_images_live(...)` = live-run wrapper (per_fov counts) called from `run_if_batch`; `write_run_context(...)` writes `if_run_context.json`; `regenerate_pub_images(run_dir, ...)` = GPU-free regenerate (rebuilds plate from per_well.csv, FOV counts from per_fov.csv, source dirs/floors/label from `if_run_context.json` or flags). Verified end-to-end via `fishsuite if-pub-images` on the panQKI run (24 channels + 24 merges + 4 composites, both sources, channel-rule + no-MIP + WT-cyto-near-zero confirmed). 2026-07-05 FULL DISPLAY-RANGE CONTROL (back-compat; new args default to prior behaviour): `build_pub_images`/`regenerate_pub_images`/`make_pub_images_live`/`write_run_context` thread `ceilings`, `dapi_floor`, `dapi_ceiling`, `per_image_ceiling`. `floors` + `ceilings` keys are now per-source-aware (`"647"`=both sources, `"647:single_plane"`=that source only; resolved by new `_resolve_source_secondary`, more-specific key wins). Signal ceiling precedence = explicit `ceilings` > per-image percentile (when `per_image_ceiling`) > shared WT-primary `ceiling_pct`. New `_dapi01(dapi, lo, hi)` applies a FIXED DAPI window (unset side = per-image min/max) — the three renderers (`_render_channels`/`_render_merge`/`_render_composite`) take `dapi_lo`/`dapi_hi`; `_render_composite` also takes per-row `his` for per-image ceilings. Applied (vmin,vmax) per source/secondary/channel logged to a header block in `qc_nuclear_dominance.txt` + summarized in `versions.txt`; new fields persisted in `if_run_context.json`. Shared-(vmin,vmax)-across-WT/KO/sec-only remains the DEFAULT. |
| `src/fishsuite/config/presets/panqki_if_intensity_wtko_2026-07-03.yaml` | NEW (2026-07-03). Annotated example `if_intensity` preset (panQKI mouse-IgG2b anti-panQKI antibody validation, WT vs QKI-KO "4-2" hESC-lineage). `if_intensity.plate_layout_csv` → the recon plate map; `well_secondary_overrides {"2":"647"}` (conflict well); `signal_channel_map {647:640,568:561,565:561}`; `dapi_channel_key 405`; `cyto_ring_px 12`; `exposure_tol_s 1e-6`; `pixel_size_um 0.2167`; shared-display 99.5/50; `nuclei` = cellpose cpsam / DirectML diam48 ds2 min700 border10; `save_publication_images: false` (IF renders its own micrographs). Reproduces the standalone 647-arm result end-to-end via `fishsuite run`. |
| `src/fishsuite/core/coloc_backfill.py` | 2026-06-19 ROTATION 'proper background' null (native port of the adversarially-validated rotation_null_prototype.py): `_compute_coloc_extras_for_image` gains `do_rotation`/`rotation_min_retention` -> returns `rotation_summary`+`rotation_draws_rows`; `backfill_run(do_rotation=False)` + module `--rotation` + `fishsuite backfill --rotation` retrofit OLD runs, writing `coloc_rotation_null_summary.csv`/`coloc_rotation_null_draws.csv` (opt-in, default OFF). NEW (2026-06-06). CPU "coloc backfill" for trusted MIAT×QKI runs that predate the native coloc-extra outputs: re-reads ONLY the QKI (antibody) channel pixels from each PLAIN VSI, reuses the run's SAVED nucleus masks + MIAT spot coords, and emits `coloc_null_draws.csv` (1000 pooled null draws), `coloc_null_summary.csv`, `coloc_radial_profile.csv` and the QKI montage PNG (`figures/07_coloc/79_coloc_qki_montage_at_miat_vs_random.png`) — WITHOUT re-segmenting or re-detecting spots. Pure core `_compute_coloc_extras_for_image(qki_2d, labels, spots_xy_by_nucleus, nucleolus_labels, *, disk_px, n_null, seed, radial_bins_px, do_null_draws, do_radial, do_montage, ...)` REUSES rna_rna's `_disk_stencil`/`_partner_null_for_nucleus`/`_annulus_stencils`/`_radial_profile_for_nucleus` (single source of truth) and reproduces the engine's spot-weighted pooling + empirical-p bit-for-bit (proven by the test's reproduction case). Thin I/O shell `backfill_run(run_dir, staging_dir=None, input_dir=None, ...)` rebuilds cfg from run_config.json, resolves the QKI index via the rna_protein antibody→rna2 shim, RECOMPUTES the analysis z-plane deterministically (`extract_channel_autofocus_with_idx` with the run's intensity_weighted + z_start/z_end — never reads z from spot_metrics, which is 0), loads the saved `nuclei_label_mask.tif`, recomputes nucleoli (`detect_nucleoli` over saved labels), and SELF-VALIDATES by reproducing the stored `per_image_summary.protein_pooled_*` (per-image PASS/FAIL table + loud WARNING on >2% mismatch; never hard-crashes). CLI: `python -m fishsuite.core.coloc_backfill --run-dir <run> --staging <staging> [--no-montage --no-radial --no-null-draws --seed 0]`. 2026-06-06 reporting-path fixes (computation core untouched; the real UD run reproduced all 11 images' protein_pooled_* to <0.004% = effective PASS, but the REPORT crashed): (1) `_gate_compare` now computes/stores the boolean `"pass"` verdict itself (gained a `tol_rel` kwarg; single source of truth) — the per-image verbose log read `gate_rows[-1]["pass"]` before `_print_gate` set it, so every image raised KeyError swallowed as a spurious "ERROR <image>"; `_print_gate` now REUSES `g["pass"]`. (2) the gate report is now pure ASCII (`Δ`→`d`, `—`→`-`) and `main()` reconfigures stdout to UTF-8/replace, so the cp1252 Windows console can't UnicodeEncodeError-crash the validation table after the CSVs are written. 2026-06-06 montage redesign (committee/pub figure; computation core, null/radial/gate untouched): the QKI montage at the same path is now the MEAN per-nucleus-normalized ENRICHMENT patch — new pure helpers `_mean_enrichment_patch(qki_2d, labels, nucleolus_labels, centers_xy_by_nucleus, half_px, *, normalize_by='nucleus_mean') -> (mean_patch, n_used)` (averages ~21×21px QKI crops centred on each MIAT spot AFTER dividing each by that spot's nucleus-mean QKI = enrichment units, removing per-section laser-brightness differences so crops are poolable; drops nucleolus-inside spots) and `_matched_random_centers(labels, nucleolus_labels, centers_xy_by_nucleus, *, seed) -> {nid:(n,2)xy}` (per-nucleus matched-count random in-nucleus draws reusing `_nucleolus_in_nucleus` + the engine's seeded sampling region; deterministic). New renderer `_render_mean_enrichment_montage(miat_patch, rand_patch, out_png, *, n_miat, n_rand, example_crops, example_vmin/max, condition_label, pixel_um, disk_r_px)` draws the two mean patches on ONE shared 1.0-centred diverging `PuOr` colorbar ("QKI/nucleus-mean (enrichment)"), annotates each panel's centre-pixel + central-disk (r=3px) enrichment (MIAT center >1 & > random ≈1), and keeps a small strip of example RAW crops from ONE image (comparable brightness) below. `backfill_run` pools the per-image patches spot-weighted across all images/conditions (valid: each crop pre-normalized) and labels the condition set + n spots; old raw-tiling `_render_montage`/`montage_pool`/`montage_vsource` removed. 600 DPI, same filename. Regenerate montage-only: `python -m fishsuite.core.coloc_backfill --run-dir <run-copy> --staging E:\Claude\fishsuite\_staging_UD_ALLARMS --no-null-draws --no-radial`. |
| `src/fishsuite/core/walkthrough_figure.py` | NEW (2026-06-07). Publication PIPELINE-WALKTHROUGH composite for the MIAT×QKI RNA-FISH project: one clean labeled 8-panel (2×4, 600 DPI) figure built from a run's OWN per-step PNGs + ONE rendered panel. Panels A–H: A nuclei (DAPI, step01), B nucleus segmentation (Cellpose outlines, step03), C MIAT RNA-FISH 640 (publication `*_yellow`), D MIAT spot detection (BigFISH-LoG, step07), E QKI IF 561 (publication `*_magenta`), F QKI thresholded intensity layer (step06 QKI threshold_on_signal), **G = NEW rendered panel** (thresholded QKI magenta field with detected MIAT spots overlaid as small yellow open rings — MIAT foci within the QKI intensity field), H merge MIAT+QKI (publication `merge_<MIAT>_<QKI>`, fallback step11_merge_all). NO statistics/coloc-result panel (Brian adds his own quant). Pure helpers (unit-tested): `threshold_qki_plane(qki_2d, threshold)` (float copy, sub-threshold→0, supra kept; non-destructive) and `render_panel_g(ax, qki_2d, threshold, spots_xy, *, vmin, vmax, pixel_um=0.13, scalebar_um=25.0, ...)` (magenta black→magenta LUT with sub-threshold pixels masked black, yellow open markers placed EXACTLY at the input (x_px,y_px), burned 25 µm scalebar; returns {image,scatter,masked,threshold,n_spots}). Panel G recompute REUSES coloc_backfill's machinery: `_resolve_plain_vsi` (imported) + `_recompute_qki_plane(cfg, vsi)` which mirrors `backfill_run`'s z-recompute via the SAME io primitives (`read_image`/`extract_channel_autofocus_with_idx`/`extract_channel_at_z`) + the rna_protein antibody→rna2 shim + `_resolve_channels` (NEVER reads z from spot_metrics, which is 0; coloc_backfill itself is UNMODIFIED). Threshold = `protein_threshold_value` from per_image_summary.csv (fallback per-image thresholds.csv → run_config `manual_antibody_min`≈1250); ceiling = `manual_antibody_max`; MIAT spots = spot_metrics rows `channel=='rna1'`. API `build_walkthrough_figure(run_dir, staging_dir=None, input_dir=None, image_key=None, out_path=None) -> png_path` (default image_key = OE g2-Dox prefix if present else first; default out = `<run>/figures/07_coloc/79_pipeline_walkthrough.png`, REPLACES the dropped mean-patch montage at slot 79). Reusable for d4/d8/d15: panels located by image_key PREFIX + known step suffixes (`_find_panel_file` with require/require_any/exclude token filters + `_resolve_image_key`/`_match_image_row`); any missing/unavailable panel self-skips with a UserWarning + placeholder (no crash, grid stays 8 axes). CLI: `python -m fishsuite.core.walkthrough_figure --run-dir <run> --staging <staging> [--image <key>] [--out <png>]`. |
| `src/fishsuite/core/singlecell.py` | NEW (2026-07-04). CPU POST-RUN single-cell (per-nucleus) treatment analysis — reads a completed run's `nuclei_metrics.csv` ONLY (no image/GPU). Pure core `compute_singlecell(nm, *, abundance_col, condition_col, group_a, group_b, exclude_secondary, metrics, chan_labels)` returns a results dict; for every meaningful per-nucleus metric it computes (1) DOSE-RESPONSE Spearman vs a per-nucleus abundance axis (default `nuclear_spot_count`) pooled+per-group + binned means, (2) MATCHED-ABUNDANCE control-vs-perturbation Mann-Whitney per abundance bin, (3) perturbation-group HETEROGENEITY by own abundance tertile (Kruskal), (4) DISTRIBUTION + per-biological-replicate Welch t; plus a SATURATION headline (associated-partner COUNT-scales-vs-FRACTION-flat) when the rotation-null assoc columns are present. GENERIC: auto-discovers numeric metric columns (skips structural/boolean flags), auto-parses (group, replicate) from the condition label (`NT_w1`->group NT/rep w1; else group=condition/rep=image), auto-picks the two groups (control-like first) — all overridable; labels prettified via the run's channel labels. I/O shell `singlecell_run(run_dir, *, abundance_col, group_a, group_b, exclude_secondary, do_excel, do_figures, out_subdir='singlecell', seed)` writes `single_cell_analysis.xlsx` (How_to_read/Dose_response/Dose_binned/Matched_abund/Heterogeneity/Distribution/Saturation), locked-style NT-vs-perturbation SuperPlots + Okabe-Ito dose scatters + a saturation composite (`figures/`), and `SINGLE_CELL_FINDINGS.md`. CLI `python -m fishsuite.core.singlecell --run-dir <run> [--abundance-col --group-a --group-b --include-secondary --no-figures --no-excel --out-subdir --seed]`. Ported from the validated sc_compute/sc_excel/sc_figures + saturation scratch scripts; reproduces the canonical run's saturation headline exactly (assoc COUNT rho=+0.538 p=3.9e-58, FRACTION flat). Wrapped by `fishsuite singlecell`. |
| `src/fishsuite/core/pixel_pattern.py` | NEW (2026-07-04). CPU POST-RUN per-nucleus PIXEL-pattern metrics (no spot calling) — like `coloc_backfill`, REUSES the run's saved nucleus masks + re-reads only raw channel pixels at the recomputed analysis z-plane (imports `_resolve_plain_vsi`/`_resolve_mask_path`/`_read_label_tiff`/`_build_nucleolus_labels` from coloc_backfill = single source of truth for resolution). Pure helpers `gini`/`top_frac`/`_radial_profile`/`_perinuc_index` + `compute_pixel_pattern_for_image(chans, labels, nucleolus_labels, ...)` compute, over the nucleoplasm (nucleus minus nucleolus, bg-subtracted), per channel (rna1/partner/dapi): mean, Gini (+raw), CV, top-5%/top-10% concentration, perinuclear (edge/center) index + a 10-bin edge->center radial profile; plus RNA1xpartner all-pixel Pearson + a decile intensity-sweep. Channels resolved generically via the rna_protein antibody->rna2 shim (`_resolve_pp_channels`); rna_only runs (no partner) still get rna1+dapi. I/O shell `pixelpattern_run(run_dir, staging_dir, input_dir, *, secondary_match, foci_floors, do_excel, do_figures, out_subdir='pixelpattern', seed)` also appends FOCI-BAND counts (partner spots/nucleus above intensity floors from spot_metrics.csv, with a secondary-only anchor) and writes `pixel_pattern_metrics.csv`, `_radial_per_nucleus.csv`, `_sweep_per_nucleus.csv`, `foci_band_summary.csv`, `nt_vs_condition_wellmeans_welch.csv`, `pixel_pattern.xlsx`, figures (SuperPlots + partner radial-with-seconly + decile sweeps + the STAIN-QC panel = partner perinuclear index vs secondary-only), and `PIXEL_PATTERN_FINDINGS.md`. `--secondary-match <substr>` designates the CLEAN secondary-only well (e.g. `well12`) — configurable because some sec-only wells are contaminated (well7-vs-well12 lesson); default = all secondary-only. STAIN QC flags whether the antibody rim is specific (rim present in secondary-only => technical). The rotation 'proper background' null remains the documented canonical coloc metric. CLI `python -m fishsuite.core.pixel_pattern --run-dir <run> [--staging --input --secondary-match --no-figures --no-excel --out-subdir --seed]`. Ported from validated compute_pixel_pattern/plot_and_report/logtm_floor_reorg; reproduces the canonical perinuclear index (NT 1.34 / KD 1.44 / sec-only 1.02). Wrapped by `fishsuite pixelpattern`. |
| `src/fishsuite/core/_superplot.py` | NEW (2026-07-04). Shared SuperPlot drawer bridge for the singlecell + pixelpattern subcommands. `get_locked_drawer()` lazily imports the LOCKED `analysis.single_condition_plots._superplot_into_axes` (searches an importable `analysis` pkg, then `$FISHSUITE_SUPERPLOT_PATH`, then the lab default `F:\Image Analysis Work\image-analysis-pipeline\python`); `superplot_into_axes(ax, df, value_col, ...)` routes group comparisons through it in `color_mode='by_condition'` (canonical look: per-nucleus dots + large per-replicate MEAN circles + violin + Welch brackets; NT/control=orange #E69F00, perturbation=blue #0072B2) and falls back to a faithful self-contained Okabe-Ito SuperPlot if the locked tree is unavailable (never import-breaks). Helpers `is_control_like`/`order_conditions_control_first`. Import is LAZY so importing fishsuite never depends on the external tree. |
| `src/fishsuite/config/presets/h9_rna_rna_test.yaml` | NEW (2026-05-13). rna_rna VALIDATION preset reusing the H9 dataset: rna=0 (640 CSU MIAT-FISH), rna2=1 (561 CSU, no probe → infrastructure test only, not biology), dapi=2. Exercises the two-channel BigFISH path + independent batch pre-scans + cKDTree spot pairing at 0.3 µm. Loadable from the GUI. |
| `src/fishsuite/config/presets/h9_miat_kd_rerun_iwfocus_2026-05-31.yaml` | NEW (2026-05-31). Committee re-run preset for H9 MIAT NT-vs-KD RNA-FISH: replaces the canonical run's hand-picked per-image MIP window with an OBJECTIVE `z_stack.mode: autofocus_maxproj` window (`focus_window_fixed_n_slices: 10` centered on the DAPI focus peak + `focus_min_intensity_frac_of_peak: 0.3` pre-filter; NO `file_overrides`/`rna_start_slice` so the RNA channel also uses the objective window). Holds floor 800, rna_only MIAT(ch0)+DAPI(ch2), cellpose cpsam diam200 ds2, batch MAD coloc, nucleolus fixed vs `CELLPOSE_FINAL_floor800_nucleolus`. KD_07 (`H9-MIAT-ASOs-_07.vsi`) excluded via `input_file_subset` (10 kept: 4 NT/3 KD/3 Sec). 2026-05-31 UPDATE: Option A (min-intensity prefilter alone) was tested offline and FAILED (picked stack-edge windows on all 10 images — the H9 stacks are bright throughout so the 0.3 prefilter disqualifies nothing). Preset now uses Option B: `autofocus_intensity_weighted: true` + `focus_central_fraction: 0.6` → all 10 windows land mid-stack (~7-17, matching the canonical hand-picked windows), no NT/KD bias. Scoping doc: `F:\Image Analysis Work\H9-MIAT-KD\RERUN_SCOPING.md`. |
| `src/fishsuite/config/presets/h9_miat_kd_0505_rerun_2026-05-31.yaml` | NEW (2026-05-31). H9 MIAT NT-vs-KD RNA-FISH preset for the EARLIER **05-05-2026** session as a potential biological replicate. Method byte-identical to the fixed 05-08 re-run (`h9_miat_kd_rerun_iwfocus_2026-05-31.yaml`): rna_only, objective `autofocus_maxproj` (intensity-weighted, fixed-N=10, central-fraction 0.6), cellpose cpsam ds2 diam200, floor 800, bigfish 0.5/100 nm, nucleolus. DIFFERENCES vs 05-08: 2-channel acquisition so `rna:0`/`dapi:1` (VSI-verified `['640 CSU','405 CSU']`, 35 z); FLAT folder with filename-encoded conditions via `conditions.filename_conditions` (`-nt_`→NT ASO, `-miat-kd_`→KD ASO) + `sec_only_files:["sec-only"]`. IMPORTANT: launch `-i` at the NESTED `...\05-05-2026\H9-MIAT-KD-ASO-05-05-2026` clean flat dir (the parent has a non-`_` duplicate subfolder that forces wrong subfolder-mode discovery). Dry-run roster VERIFIED: 5 KD / 4 NT / 4 Sec-Only (13). Scope doc: `F:\Image Analysis Work\H9-MIAT-KD\PRESETS_0505_0506_SCOPE.md`. |
| `src/fishsuite/config/presets/h9_miat_kd_0506_descriptive_2026-05-31.yaml` | NEW (2026-05-31). H9 MIAT RNA-FISH preset for the EARLIER **05-06-2026** session, UNLABELED. Filenames carry NO condition label, so ALL 11 images run as ONE descriptive group `subfolder_conditions:{"":"05-06 (unlabeled)"}` (no NT/KD split — splitting by MIAT signal would be circular). Same cloned 05-08 method; 2-channel `rna:0`/`dapi:1` (VSI-verified, 30 z). Mod-time/size clue (reported, not acted on): `_10`/`_11` are a separate later acquisition block and match the 05-05 Sec-only size signature → possibly sec-only controls; left as-is pending Brian's lab record. Dry-run roster VERIFIED: 11 images, one group. Scope doc as above. |
| `src/fishsuite/config/presets/miat_oe_ud_g2_rna_only_2026-06-03.yaml` | NEW (2026-06-03). Undifferentiated hESC (H9-type "g2") Dox-inducible MIAT-OVEREXPRESSION RNA-FISH preset: g2 +Dox = MIAT OE, g2 noDox = Control. rna_only MIAT(ch0/640 CSU → yellow) + DAPI(ch2/405); QKI (ch1/561 CSU panQKI IF) present but NOT analyzed (noted for a later rna_protein run). Cloned method from `h9_miat_kd_rerun_iwfocus_2026-05-31.yaml`: objective `autofocus_maxproj` (fixed-N=10, intensity-weighted, central 0.6), cellpose cpsam ds2, batch MAD coloc, bigfish 0.5/100 nm. Per-dataset facts: VSI-verified 3-ch / 78-z / **0.13 µm/px** (so `cellpose_diameter_px:100` = same 13 µm nucleus as H9's 200@0.065; `min_area_px:4000`). Subfolder conditions (`...noDOX`→Control, `...wDOX`→MIAT OE), `input_file_subset` pins ONLY the 4 real .vsi (excludes derived C1/C2/C3 MAX .tif + overlay RGB). **Starting floor 300** (Control p99≈227/p99.9≈1058 vs OE p99≈1095/p99.9≈2853 → 300 catches low-baseline noDOX puncta, excludes background; RE-TUNE on bright OE per Sam's method). Dry-run roster VERIFIED: 2 Control / 2 MIAT OE (n SMALL — first-pass). Scope doc: `F:\Image Analysis Work\MIAT OE\MIATOE_SCOPE.md`. |
| `src/fishsuite/config/presets/h9_rna_rna_test_labeled.yaml` | NEW (2026-05-13). rna_rna validation preset WITH demo channel labels (`dapi_label="DAPI"`, `rna_label="MIAT-Cy5"`, `rna2_label="Empty-Cy3"`) so the labels feature can be exercised end-to-end. Identical numerics to `h9_rna_rna_test.yaml`. 2026-05-13 per-channel-overrides validation: adds `foci.rna2_overrides.threshold_multiplier=0.7` (stricter than shared `threshold_multiplier=0.5`) so the preset exercises the override path. Validated on H9 MIATKD_07: RNA1 (tm=0.5) detects 1719 spots; RNA2 (tm=0.7) detects 373 spots (vs 1744 when both at 0.5). Effective values recorded in thresholds.csv. |

## Scripts

| Path | Description |
|---|---|
| `scripts/regen_walkthrough_threshold_floor.py` | NEW (2026-06-02). Standalone re-render of the H9 MIAT-KD walkthrough THRESHOLD panels at the spot floor (800), overwriting the misleading pixel-coloc-MAD-thresholded step05/06/08 panels in the two existing runs `RERUN_IWFOCUS_floor800_no07_20260531-100336` and `RERUN_0505_corrected_labels_floor800_20260531-112658`. Reproduces step05 (MIAT>=800 yellow-on-black), step06 (overlay on grayscale, base floor 800*0.75), and step08 (saved floor-filtered spots on the 800-gated background); titles every panel "MIAT-640 ≥ spot floor 800". Maps walkthrough stem -> .vsi via the per-image `spot_metrics.csv` `image` column (robust to the 0505 run's corrected/swapped condition labels), MAX-PROJECTs the SAME per-image objective z-window parsed from `<run>_run.log` ("window=[a,b]", 0-indexed inclusive), thresholds at 800. NO GPU / no pipeline re-run; reads raw `.vsi` via `fishsuite.core.io.read_image`. Does NOT touch spot detection, the floor-filter, pixel_coloc, or any quantitative CSV/xlsx. Run: `conda run -n fishproc_dml python scripts/regen_walkthrough_threshold_floor.py`. Originals backed up to each run's `pipeline_walkthrough_ORIG_threshold_panels_backup/`. |
| `F:\Image Analysis Work\BIN1\make_bin1_presentation_walkthrough_2026-06-03.py` (out-of-repo) | NEW (2026-06-03). DISPLAY-ONLY presentation figure: a 2-row × 5-column stepwise RNA-FISH walkthrough for the BIN1 d8cMyo QKI-KO_4-2-vs-WT committee run (`RUN_FINAL_introns1600_samplerfix_20260529-220517`, rna_protein) on two images (KO 09 + WT 06). Columns: (1) Merge — XRN2 yellow (1454/4000) + BIN1-introns magenta (1600/2200) + DAPI blue (1200/30000) additive, 25µm scale bar; (2) Nuclear segmentation — DAPI blue + nuclei outlines from the SAVED committee `masks/<stem>__nuclei_label_mask.tif` via `draw_nuclei_outlines` (REUSED, NOT re-segmented); (3) BIN1 introns ≥1600 magenta-on-black; (4) XRN2 ≥1454 yellow-on-black; (5) Overlap — XRN2-only yellow / BIN1-only magenta / BOTH WHITE. Same autofocus plane per image the run used (DAPI intensity-weighted autofocus window [8,72] → z-lock via `extract_channel_autofocus_with_idx`+`extract_channel_at_z`; KO09 z=20, WT06 z=18). Reuses fishsuite `apply_lut`/`merge_rgb_additive`/`draw_nuclei_outlines`/`burn_scale_bar`/`burn_corner_title`/`save_png`. Composite assembled with matplotlib at 600 DPI (`BIN1_XRN2_walkthrough_KO09_WT06.png`) + 10 individual panels in `panels\`. DISPLAY-only dilation (magenta r2, white r3) for slide visibility — reported pixel counts are UNDILATED (KO09 white-both=125, WT06=44). The white overlap is a DISPLAY overlap at figure floors, NOT the quantified MAD/Pearson coloc. No GPU/cellpose/CSV; committee run dir + reused masks byte-untouched (verified). Output → `...\_presentation_walkthrough_figure_2026-06-03\`. Results doc: `F:\Image Analysis Work\BIN1\BIN1_XRN2_PRESENTATION_WALKTHROUGH_2026-06-03.md`. |
| `F:\Image Analysis Work\BIN1\make_bin1_retention_compartment_v6b_repr_2026-06-04.py` (out-of-repo) | NEW (2026-06-04). v6b INTEGRITY FIX of v6 — re-picks the col-B zoom cells from CHERRY-PICKED (v6 KO nucleus 20 = 9 foci vs 2.9/nuc field avg; WT max-cyto cell) to COUNT-MATCHED REPRESENTATIVE cells so the comparison isolates LOCALIZATION not abundance. Both chosen cells = 3 total foci (near field median): **KO 02 nucleus 14 = 3 nuclear / 0 cyto**; **WT 05 nucleus 1 = 2 nuclear / 1 cyto** (composition computed at runtime + burned into honest labels "N foci: X nuclear / Y cyto"; verified geometrically — WT's 1 cyto focus falls outside the nucleus mask). Col A (full field, 90% vs 57% nuclear) UNCHANGED from v6. Same magenta=nuclear / orange=cytoplasmic(#E69F00) coding, white nucleus outline (saved masks), 5µm zoom bar. HONEST read: at matched abundance the per-cell difference is REAL but SUBTLE (a 1-focus localization shift) — the magnitude is the POPULATION fraction (col A) + the pooled paired-t, not the single cell. DISPLAY-ONLY; no analysis/config edit, no re-run, no GPU; committee dir + masks byte-untouched (verified); v6 + all prior preserved. Output: `BIN1_intron_RETENTION_compartment_KO02_WT05_v6b_repr.png` (600 DPI) + 4 `panels\<stem>__v6b_{A_fullfield_compartment,B_zoom_repr}.png` + `_manifest_v6b.json`. Results doc: `F:\Image Analysis Work\BIN1\BIN1_INTRON_RETENTION_COMPARTMENT_V6B_REPR_2026-06-04.md`. |
| `F:\Image Analysis Work\BIN1\make_bin1_retention_compartment_v6_2026-06-04.py` (out-of-repo) | NEW (2026-06-04). v6 RETENTION-EMPHASIS figure — colors BIN1 intron foci by COMPARTMENT to show the KO>WT nuclear-retention difference distinctly (drops the v5 rich intron channel, which showed absolute intensity and made the brightest field WT 05 look misleadingly strong). 2 rows (KO 02 top, WT 05 bottom) × 2 cols: (A) full field — faint DAPI + WHITE nucleus outlines (saved masks) + intron foci markers colored NUCLEAR=magenta / CYTOPLASMIC=orange (#E69F00 Okabe-Ito) + per-row "% nuclear (nuc/cyto)" label + 25µm bar; (B) representative cell zoom — KO nucleus 20 (all-nuclear magenta = retention) vs WT cell nucleus 5 (magenta nuclear + orange cytoplasmic foci OUTSIDE the outline = export), NN-upscaled, 5µm bar. Compartment from spot_metrics rna1 in_nucleus/in_cytoplasm flags (assigned, floaters excluded): KO02 86 nuc / 10 cyto = 90% nuclear; WT05 17 nuc / 13 cyto = 57% nuclear (CONFIRMED, in labels). Per-nucleus TOTAL foci similar (KO 2.91 / WT 2.50) — the FRACTION differs; quantified result = pooled cross-dataset paired-t, not this single field. Cytoplasm was a runtime Voronoi (40px, measure_nc_ratio), NOT a saved mask → only nuclei outlines drawn; cyto foci placed by saved flag/position. No analysis/config edit, no re-run, no GPU; committee dir + masks byte-untouched (verified); v1–v5 + diagnostic + style-options preserved. Output: `BIN1_intron_RETENTION_compartment_KO02_WT05_v6.png` (600 DPI) + 4 `panels\<stem>__v6_{A_fullfield,B_zoom}_compartment.png` + `_manifest_v6.json`. Results doc: `F:\Image Analysis Work\BIN1\BIN1_INTRON_RETENTION_COMPARTMENT_V6_2026-06-04.md`. |
| `F:\Image Analysis Work\BIN1\make_bin1_presentation_walkthrough_v5_style4_2026-06-04.py` (out-of-repo) | NEW (2026-06-04). v5 (Brian-approved FINAL) of the BIN1 intron×XRN2 2×6 walkthrough — NEW density-matched field pair **KO 02 (top) vs WT 05 (bottom)** (both well 3) + **Style-4 intron display** (rich continuous intron channel + cyan-ringed called foci). Foci = spot_metrics rna1 assigned (nuc OR cyto, floaters excluded): KO02 96 (86 nuc+10 cyt, 2.91/nucleus, 77 XRN2-coincident), WT05 30 (17 nuc+13 cyt, 2.50/nucleus, 11 coincident) — density-matched per-nucleus, counts read at runtime + in titles. Cols: (1) Merge DAPI-blue + XRN2-yellow(≥1454) + intron CHANNEL magenta at Style-4 window 673–p99.5-nuclear (KO 673–1254, WT 673–1152), no foci rings; (2) segmentation (saved-mask white outlines, NOT re-segmented); (3) Style-4 — continuous magenta intron channel + CYAN-ringed called foci ("channel (673–p99.5) + n=96/30 called foci"); (4) XRN2≥1454 yellow; (5) Overlap — foci markers WHITE if XRN2≥1454 within 3px else MAGENTA on faint intron+XRN2 backdrop + white zoom rectangle (77/96, 11/30); (6) zoom median-coincident nucleus (KO02 nuc32, WT05 nuc1) rich-channel+foci, NN-upscaled. Intron ANALYSIS floor unchanged (1600); magenta channel is a DISPLAY window only (shows diffuse/near-bkgd; cyan rings + white/magenta markers denote the quantified foci). White overlay = display coincidence; quantified coloc = paired_fraction_rna1_at_0p3um / mean_protein_enrichment_at_rna1_spots. Same autofocus plane (KO02 z=22, WT05 z=17), fishsuite helpers. No analysis/config edit, no re-run, no GPU; committee dir + masks byte-untouched (verified); v1–v4 + diagnostic + style-options preserved. Output: `BIN1_XRN2_walkthrough_KO02_WT05_v5_style4.png` (600 DPI) + 8 `panels\<stem>__v5_*.png` + `_manifest_v5.json`. Results doc: `F:\Image Analysis Work\BIN1\BIN1_XRN2_PRESENTATION_WALKTHROUGH_V5_STYLE4_2026-06-04.md`. |
| `F:\Image Analysis Work\BIN1\make_intron_style_options_2026-06-04.py` (out-of-repo) | NEW (2026-06-04). DISPLAY-ONLY intron-style COMPARISON (Brian to pick) for KO 09 + WT 07 of the committee run — 2 rows × 4 style columns, same magenta LUT + faint DAPI for comparability. Styles: (1) detected foci small magenta markers (v4 baseline); (2) SAME foci bold/glow (larger marker + additive halo, same n); (3) CONTINUOUS contrast-stretched intron channel `apply_lut(bin1, magenta, floor=673 MAD, ceil=p99.5-nuclear)` (KO 673–1160, WT 673–1185) — the "more signal" look but mostly diffuse/near-bkgd (NOT a hard threshold, NOT new detections); (4) Style-3 channel + the called foci ringed CYAN (shows diffuse channel AND what was quantified). Foci = spot_metrics rna1 assigned (nuc OR cyto, floaters excluded): KO09 63, WT07 17 (read at runtime, in titles). Analysis floor 1600 NOT lowered; Style 3/4 are a DISPLAY window only. Results doc ALSO tabulates all 8 fields' `total_spots_rna1` (KO02=96 densest, KO03=35, KO08=67, KO09=63, WT04=21, WT05=30, WT06=43, WT07=17 — confirmed) so a denser field (KO 02) is a genuine "more foci" option. Same autofocus plane / saved masks (NOT re-segmented) / fishsuite helpers (`draw_spot_markers`/`apply_lut`/etc). No analysis/config edit, no re-run, no GPU; committee dir + masks byte-untouched (verified); v1–v4 + diagnostic preserved. Output: `..._presentation_walkthrough_figure_2026-06-03\INTRON_STYLE_OPTIONS_KO09_WT07_2026-06-04.png` (600 DPI) + 8 `panels\<stem>__style_s1..s4.png` + `_intron_style_options_data_2026-06-04.json`. Results doc: `F:\Image Analysis Work\BIN1\INTRON_STYLE_OPTIONS_2026-06-04.md`. |
| `F:\Image Analysis Work\BIN1\make_bin1_presentation_walkthrough_v4_spots_2026-06-04.py` (out-of-repo) | NEW (2026-06-04). v4 of the BIN1 intron×XRN2 2×6 walkthrough — SPOT-BASED intron representation (Brian-approved after the 2026-06-04 diagnostic showed the pixel-threshold mask undersells real detected foci). Introns now drawn as DETECTED FOCI markers from `spot_metrics.csv` (channel==rna1), using the ASSIGNED foci (in_nucleus OR in_cytoplasm; floaters excluded): KO09 63 (55 nuc+8 cyt), WT07 17 (13 nuc+4 cyt) — exact counts read at runtime + burned into titles. Cols: (1) Merge DAPI-blue + XRN2-yellow(≥1454) + intron foci MAGENTA markers; (2) segmentation (unchanged, saved-mask white outlines); (3) BIN1 introns as magenta foci markers on faint DAPI, "n=63/17 detected foci"; (4) XRN2≥1454 yellow field; (5) Overlap — each focus WHITE if XRN2≥1454 within 3px of its centroid else MAGENTA (KO09 42/63, WT07 9/17 coincident; honest KO>WT) + white zoom-source rectangle; (6) Zoom on median-coincident nucleus (KO09 nuc13, WT07 nuc7) NN-upscaled. Intron floor NOT lowered (per diagnostic); XRN2 field stays ≥1454. White overlay is DISPLAY coincidence; quantified coloc = paired_fraction_rna1_at_0p3um / mean_protein_enrichment_at_rna1_spots. Same autofocus plane / saved masks (NOT re-segmented) / fishsuite helpers (incl. `draw_spot_markers`). No analysis/config edit, no re-run, no GPU; committee dir + masks byte-untouched (verified); v1–v3 + diagnostic preserved. Output (same folder, new names): `BIN1_XRN2_walkthrough_KO09_WT07_v4_spots.png` (600 DPI) + 8 `panels\<stem>__v4_*.png` + `_manifest_v4.json`. Results doc: `F:\Image Analysis Work\BIN1\BIN1_XRN2_PRESENTATION_WALKTHROUGH_V4_SPOTS_2026-06-04.md`. |
| `F:\Image Analysis Work\BIN1\make_intron_display_diagnostic_2026-06-04.py` (out-of-repo) | NEW (2026-06-04). DISPLAY-ONLY diagnostic answering "is BIN1-intron floor 1600 over-cutting real introns, or does the pixel-mask just undersell detected foci?" for the committee run (`RUN_FINAL_introns1600_samplerfix_20260529-220517`, KO 09 + WT 07). On the run's autofocus intron plane (z-lock) + saved nuclei mask + saved `spot_metrics_rna1.csv`: reports within-nucleus vs whole-image intron percentiles, a floor sweep [1600/1200/1000/800/673(MAD)/p99.5] showing in-nucleus intron PIXELS passing AND DETECTED spots with peak≥floor, and a pixel-mask@1600 vs detected-spots-on-DAPI comparison. KEY FINDING: every saved detected intron focus has peak ≥1600 (KO09 72/72 min-peak 1607; WT07 26/26 min-peak 1603) → floor 1600 drops ZERO detected foci; nuclear p99≈1030/1080 so the diffuse intron channel barely clears 1600 at pixel level (sparse mask is a DISPLAY artifact, not lost signal). VERDICT: 1600 is not over-cutting; show introns as DETECTED SPOTS (honest, spot-based) rather than the pixel-threshold mask. No analysis/config edit, no re-run, no GPU; committee dir + masks byte-untouched (verified). Output → `..._presentation_walkthrough_figure_2026-06-03\INTRON_DISPLAY_DIAGNOSTIC_KO09_WT07_2026-06-04.png` (600 DPI) + `_intron_diagnostic_data_2026-06-04.json`. Results doc: `F:\Image Analysis Work\BIN1\INTRON_DISPLAY_DIAGNOSTIC_2026-06-04.md`. |
| `F:\Image Analysis Work\BIN1\make_bin1_presentation_walkthrough_v3_zoom_WT07_2026-06-03.py` (out-of-repo) | NEW (2026-06-03). v3 of the BIN1 intron×XRN2 2×6 zoom walkthrough — Brian swapped the WT field from WT 06 (v2) to **WT 07** (`07_WT well6.vsi`); KO 09 unchanged. Identical pipeline to v2 (floors XRN2 1454 / introns 1600, LUTs, autofocus z-lock window [8,72], full-field display dilation cols 3+5, UNDILATED NN-upscaled zoom, col-5 marker rectangle, labels, 25µm/5µm scale bars); derived from v2 by changing only the WT image entry + output names. WT 07 is a SPARSE field (8 nuclei, z=18, 68 intron-px, 21 white-coincidence-px) rendered honestly (not densified). Zoom nucleus = median-by-white among coincidence nuclei: KO09 label 4 (8 white px, dist 2/8/17 over 15), WT07 label 5 (2 white px, dist 1/4/6 over 4 coincidence nuclei; intron-only fallback NOT needed); honest KO>WT contrast (8 vs 2). Reuses SAVED committee `WT__07_WT_well6__nuclei_label_mask.tif` (NOT re-segmented). No GPU/cellpose/CSV; committee dir + masks byte-untouched (verified); v1+v2 outputs preserved. Output (same folder, new names): `BIN1_XRN2_walkthrough_KO09_WT07_v3_zoom.png` (600 DPI) + `panels\WT__07_WT_well6__06_overlap_zoom.png` + `_manifest_v3.json`. Results doc: `F:\Image Analysis Work\BIN1\BIN1_XRN2_PRESENTATION_WALKTHROUGH_V3_WT07_2026-06-03.md`. |
| `F:\Image Analysis Work\BIN1\make_bin1_presentation_walkthrough_v2_zoom_2026-06-03.py` (out-of-repo) | NEW (2026-06-03). v2 of the BIN1 intron×XRN2 presentation walkthrough — adds a ZOOMED INSET column (now a 2-row × 6-column grid; v1 5-col script + outputs kept). Col 6 "intron × XRN2 (zoom)" = tight square crop around ONE representative nucleus per image, chosen as the MEDIAN-by-white-coincidence nucleus among nuclei with ANY coincidence (honest typical, not extreme): KO09 → label 4 (8 white px; dist min/med/max 2/8/17 over 15 coincidence-nuclei), WT06 → label 4 (3 white px; dist 2/4/15 over 8) — preserves the honest KO>WT contrast. Zoom uses the UNDILATED true-pixel overlap (XRN2-only yellow / BIN1-only magenta / both WHITE), NEAREST-NEIGHBOR upscaled (~×4-5 to ~720px) so each real pixel is a visible block, faint gray nucleus outline, 5µm scale bar, labelled with the white-px count. The full-field Overlap panel (col 5) gains a thin WHITE marker rectangle drawn from the SAME bbox the crop used. Same autofocus plane / saved-mask reuse / fishsuite helpers as v1; full-field cols 3+5 keep v1 display dilation, the ZOOM is undilated. White = DISPLAY/illustrative coincidence at figure floors, NOT the quantified MAD/Pearson coloc. No GPU/cellpose/CSV; committee dir + masks byte-untouched (verified). Output (same folder, new names): `BIN1_XRN2_walkthrough_KO09_WT06_v2_zoom.png` (600 DPI) + `panels\{KO__09,WT__06}..._06_overlap_zoom.png` + `_manifest_v2.json`. Results doc: `F:\Image Analysis Work\BIN1\BIN1_XRN2_PRESENTATION_WALKTHROUGH_V2_ZOOM_2026-06-03.md`. |
| `F:\Image Analysis Work\BIN1\regen_bin1_xrn2_threshold_floor_2026-06-03.py` (out-of-repo) | NEW (2026-06-03). DISPLAY-ONLY re-render of the BIN1 d8cMyo QKI-KO_4-2-vs-WT committee run (`RUN_FINAL_introns1600_samplerfix_20260529-220517`, mode rna_protein) walkthrough THRESHOLD panels for a cleaner figure: XRN2 (ch0, yellow) step05/06 at display floor **1454** (the data-driven pixel-coloc MAD from `thresholds.csv` `protein_threshold_value`, vs the run's 1100 floor) labelled "XRN2 ≥ floor 1454", plus BIN1-Introns (ch1, magenta) at unchanged floor 1600 labelled "BIN1 Introns ≥ floor 1600", for all 8 images. Faithfully reproduces the run's z-mode `autofocus` (SINGLE in-focus plane, z-locked to DAPI's intensity-weighted autofocus pick within per-file window [8,72]) by calling fishsuite's own `extract_channel_autofocus_with_idx` + `extract_channel_at_z`, and mirrors `output.save_walkthrough_bundle_rna_rna._channel_steps` (step05 color-on-black + title; step06 `apply_lut` floor*0.75 base + 50/50 blend + title + 25µm/font-52 scale bar). Also builds `COMPARISON_XRN2_1100_vs_1454_KO__02.png` (original committee step05 READ read-only vs new 1454 panel). NO segmentation/spots/CSV/GPU; committee run dir is READ-ONLY (verified byte-untouched). Output → `...\_example_walkthrough_xrn2_floor1454_2026-06-03\` (32 panels + comparison + `_manifest.json`). Proof of tightening: XRN2 positive pixels drop 56–63% across all 8 images (KO__02: 58,216→24,695). Results doc: `F:\Image Analysis Work\BIN1\XRN2_FIGURE_REGEN_2026-06-03.md`. |

## Tests

| Path | Description |
|---|---|
| `tests/test_smoke.py` | Package import, preset YAML validation, coloc metrics sanity. 2026-05-31: added `test_discover_filename_conditions_flat_folder` (locks the new flat-folder `filename_conditions` substring map — NT/KD split, sec-only not relabelled, empty-map back-compat). 2026-05-13 channel-detect + per-file-tree pass: added `test_fluorophore_for_channel_name_known_lasers` (verifies 640/561/488/405 → Cy5/Cy3/GFP/DAPI heuristics + graceful fallback for unknown names like "Brightfield"), `test_scan_input_dir_tree_subfolder_layout` (verifies KO/WT subfolder tree walking + extension filtering + underscore-prefix skip), `test_runner_input_file_subset_filter` (end-to-end: writes a YAML with `input_file_subset=["imgA.vsi"]`, monkeypatches `get_mode` to capture per-image calls, asserts only imgA was dispatched; also asserts a subset that matches zero files raises `RuntimeError`). Total tests: 20. |
| `tests/test_output.py` | NEW (2026-05-11). Output module smoke: LUT colors, scale-bar burn, label-TIFF roundtrip, walkthrough + publication bundle writers. 2026-05-12: added `test_sanitize_condition_for_filename`, `test_sec_only_does_not_pollute_batch_cache` (locks in sec-only consult-but-don't-update behavior), `test_scale_bar_text_has_black_outline_pixels` (verifies bold-text outline is drawn). 2026-05-13 BIN1 Run1 issue 4: added `test_lut_name_to_weights`, `test_publication_image_lut_selection_changes_color`, `test_publication_image_lut_changes_output_filename`. 2026-05-15 walkthrough audit pass: added `test_save_walkthrough_bundle_emits_steps_07_08_09_when_inputs_present` (locks in rna_only step07/08/09 filenames + 9-panel total when spots+cyt_labels supplied) and `test_save_walkthrough_bundle_rna_rna_emits_extended_steps` (locks in all 15 rna_rna walkthrough step filenames including step09 active_TSS, step10 cytoplasm_mask, step11 merge_all). 2026-05-29 FIXED: `test_save_walkthrough_bundle_rna_rna_emits_extended_steps` now PASSES — the writer's stale `__step09_nuclear_overlap_spots` filename was renamed to `__step09_active_TSS` in output.py to match the test + the feature's own "Active TSS" intent (root cause was the filename string, not the test). Full suite: 71 passed, 0 failed (run per-file/per-dir: bare `pytest` with no path args can hang in the DML env). Total tests: 22. |
| `tests/test_rna_protein_depth.py` | rna_protein analysis-depth parity tests (model-free `otsu` segmentation + BigFISH on a synthetic 3-channel stack; no GPU). Asserts rna_protein analyzes the antibody channel at the same depth rna_rna gives rna2 and relabels rna2-*→protein-*. 2026-05-29: added `test_partner_intensity_per_nucleus_populated` (REGRESSION for the intensity-coloc all-NaN bug — with `foci.compute_partner_intensity=True`, asserts the per-nucleus `rna2_local_mean_at_rna1_spots`/`_enrichment_at_rna1_spots` + reciprocal, and the rna_protein-relabeled `protein_local_mean_at_rna1_spots` etc., are FINITE for spot-bearing nuclei and NaN only when a channel has no spots; fails on the pre-fix `_by_nid`-before-column ordering) and `test_partner_intensity_off_by_default` (gating contract: columns ABSENT when the flag is False). |
| `tests/test_partner_null_coloc.py` | (unchanged by 2026-06-19 rotation work; see test_partner_rotation_null.py). NEW (2026-06-05). 10 GPU-free unit tests for the pipeline-native per-nucleus random-position NULL coloc (SCIENTIFIC-CORE). (a) HELPER-LEVEL known answers: `_disk_stencil(3.0)` keeps 29 in-radius offsets; bright-region spots vs whole-nucleus null give enrichment >1.5 + z>0 (obs=100 vs field-mean ~55); UNIFORM partner -> enrichment exactly 1.0; same-seed determinism (identical null distribution). (b) `test_nucleolus_exclusion_changes_null_sampling`: excluding the dim "nucleolus" half from the null positions RAISES null_mean (-> lower enrichment), observed unchanged. (c) END-TO-END through `rna_rna.run_one` on a synthetic 3-channel stack (3 nuclei each with a DAPI-poor + QKI-poor central nucleolar hole): null columns present + finite, per-image pooled rollup sane (0<p<=1), deterministic across runs, nucleolus-exclusion ON vs OFF changes the pooled enrichment, the rna_rna nucleolus columns (`nucleolus_area_px`/`nucleolus_fraction_of_nucleus`/per-spot `in_nucleolus`) are emitted (Feature-2 wiring), and DEFAULTS-OFF byte-equivalence (no null columns/keys; `assert_frame_equal` of nuclei + per_image dict equality excluding the wall-clock `runtime_s`). All 10 pass. 2026-06-06: +2 tests (now 12) for the NATIVE null-draws carrier — `test_save_partner_null_draws_emits_extra` (with `foci.save_partner_null_draws=True`, `res.extra["coloc_null_draws"]` has exactly `n_null` rows, cols `image/condition/iter/pooled_null_value/pooled_obs`, `mean(pooled_null_value)` == reported per-image pooled null mean, and the reconstructed empirical p `(#draws>=obs+1)/(n+1)` exactly matches `rna2_pooled_null_p_empirical_at_rna1_spots`) and `test_save_partner_null_draws_default_off_absent` (flag default OFF -> key absent even with the null feature ON). |
| `tests/test_footprint_association.py` | NEW (2026-07-07). 14 GPU-free tests for the MIAT×QKI ASSOCIATION metrics (spec `_SPEC_association_analysis_2026-07-06.md`), gated on `foci.compute_footprint_enrichment` (default OFF). (a) HELPER-LEVEL `_sample_qki_at_miat_footprint` on a synthetic MIAT-Gaussian-on-QKI image: constant-QKI footprint mean == the constant (any footprint shape); a wider MIAT spot (larger sigma) yields a >2× LARGER half-max footprint area (size-scaling proof); a QKI x-gradient → footprint mean == the gradient value at the spot (samples at the right pixels); a flat/no-contrast MIAT crop → fitted-radius DISK fallback returns a finite value (never NaN); empty spots → empty arrays + all-False union mask. (b) HELPER-LEVEL MOC/ICQ via `compute_coloc_metrics`: ICQ>0 correlated / <0 anti-correlated, MOC (cosine) higher for correlated. (c) END-TO-END through `rna_rna.run_one` on the shared synthetic 3-channel stack: per-spot/per-nucleus/per-image columns present (rna2 rows NaN for footprint cols); `qki_footprint_enrichment == qki_at_miat_footprint / rna2_nuclear_mean` to float precision; `qki_assoc_ratio_continuous == mean` per-spot enrichment; `coloc_moc`/`coloc_icq` exactly alias `coloc_cosine_overlap_rna1_rna2`/`coloc_li_icq_rna1_rna2`; floor-gated column named `qki_assoc_ratio_gated_<floor>`, present only with a floor, and ≤ continuous per nucleus; RNG-free determinism. (d) DEFAULTS-OFF byte-equivalence (no new columns/keys; `assert_frame_equal` of nuclei + per_image dict equality). All 14 pass; full fishsuite suite 239 passed. |
| `tests/test_partner_radial_profile.py` | NEW (2026-06-06). 7 GPU-free unit tests for the pipeline-native radial QKI-around-MIAT profile. (a) HELPER-LEVEL: `_annulus_stencils([3,6,9,12])` returns disjoint non-empty rings (ring 0 incl. center, ring i in `(edge[i],edge[i+1]]`); a bright ring painted at `d in (6,9]` around a single spot makes the MATCHING annulus (ring index 2) the enrichment peak (>1.5); same-seed determinism (identical per-ring null mean/sd); `test_radial_nucleolus_exclusion_raises_null_floor` (excluding the dim void from the random sampling positions RAISES the per-ring null floor, observed unchanged — mirrors the disk-null nucleolus test). (b) END-TO-END through `rna_rna.run_one` on the synthetic 3-channel stack: default OFF -> `coloc_radial_profile` ABSENT in extra; ON -> DataFrame with one row per ring + cols `image/condition/ring_um/obs_mean/null_mean/null_sd/enrichment/z/n_spots` and finite enrichment; deterministic across two runs. All 7 pass. |
| `tests/test_partner_rotation_null.py` | NEW (2026-06-19). 17 GPU-free TDD tests for the rotation 'proper background' null. Helper-level: positive control (enr>1, sig), negative (enr~1, n.s.), equivalence (no-compartment -> rotation~=position), ordering (compartment -> rotation more conservative), elliptical low-retention keep-N recovery, uniform-field no-redraw-bias guard, determinism, association-fraction above/below chance, translation positive control. End-to-end through run_one: columns present+finite, deterministic, defaults-OFF byte-equivalence, save_partner_rotation_null_draws -> extra['coloc_rotation_null'], translation companion wiring. |
| `tests/test_coloc_backfill.py` | 2026-06-19 added 4 rotation-null pure-core tests (`do_rotation=True`: bright-at-spots enriched, uniform==1, deterministic, default-off-absent). NEW (2026-06-06). 11 GPU-free TDD tests for `core/coloc_backfill.py`. (a) PURE-CORE unit tests on synthetic arrays: QKI bumped at MIAT-spot pixels → pooled enrichment >1.2, z>0, p<0.05; UNIFORM partner → enrichment exactly 1.0, z NaN, empirical p 1.0; same-seed determinism (identical pooled null vector); null_draws shape = n_null + empirical-p reconstruction matches summary; nucleolus exclusion (label image) raises the null floor → lower enrichment; bright ANNULUS at radius ~5px → matching radial ring (index 2) is the enrichment peak; montage crops at MIAT spots brighter than random crops + deterministic + fixed crop shape. (b) REPRODUCTION through the real engine: run `rna_rna.run_one` on a synthetic 3-channel stack with `save_partner_null_draws`+radial+nucleolus-exclusion ON, pull the SAME qki_2d/labels/nucleolus_labels/spots1 out of `res.qc`, feed the pure core, and assert the pooled null VECTOR matches `res.extra["coloc_null_draws"]` to atol 1e-9, pooled obs/null_mean/enrichment/z/p match `res.per_image.rna2_pooled_*`, and the radial rows match `res.extra["coloc_radial_profile"]` — proving the core IS the engine math (single source of truth). (c) `backfill_run` API/signature smoke + clean error on a run_dir with no run_config.json. All 11 pass; full fishsuite suite 113 passed. 2026-06-06 +4 reporting-path regression tests (now 15): `test_gate_compare_sets_pass_key` (`_gate_compare` returns a bool `"pass"`: True on match, False on gross mismatch — reproduces the KeyError bug), `test_print_gate_reuses_pass_single_source_of_truth` (flipping a row's `pass` flips `_print_gate`'s n_fail — proves reuse not recompute), `test_print_gate_is_ascii_only_and_does_not_raise` (report `.encode("ascii")` succeeds, no `Δ`), `test_print_gate_writes_to_cp1252_stream_without_error` (redirects stdout to a STRICT cp1252 stream — faithful Windows-console reproduction — and asserts no UnicodeEncodeError). Full suite 117 passed. 2026-06-06 +8 mean-enrichment-montage TDD tests (now 23): `_mean_enrichment_patch` center>edge & center>1 on a bright-blob image (n_used counts all spots; 17×17 shape), FLAT field → patch ≈1.0 everywhere, unsupported `normalize_by` raises, nucleolus-excluded normalization lowers central enrichment vs whole-nucleus; `_matched_random_centers` determinism + matched per-nucleus count + seed-sensitivity + all draws stay inside their nucleus; matched-random patch is lower-center AND flatter (smaller center-to-mean contrast) than the MIAT patch; render smoke test writes a non-empty 600-DPI PNG at the canonical filename with the example raw-crop strip. Full fishsuite suite 125 passed. |
| `tests/test_walkthrough_figure.py` | NEW (2026-06-07). 11 GPU-free TDD tests for `core/walkthrough_figure.py`. (1) PURE panel-G logic on synthetic arrays: `threshold_qki_plane` zeroes sub-threshold + keeps supra (boundary `==` kept), deterministic + non-destructive; `render_panel_g` places markers EXACTLY at supplied (x_px,y_px) (`scatter.get_offsets()` equality), masks the sub-threshold field (`np.ma` mask == `qki<thr`), graceful on empty spots. (2) resolution helpers: default image_key prefers the OE (`MIAT_OE`) prefix over Nog; explicit passthrough; `_match_image_row` maps panel-prefix → full-VSI per_image row + its `protein_threshold_value`. (3) SMOKE on a synthetic run-dir (7 composable PNGs + distractors + minimal spot_metrics/per_image_summary/run_config + monkeypatched `io.read_image` for a fake VSI under staging): `build_walkthrough_figure` writes a non-empty 600-DPI PNG at the default `figures/07_coloc/79_pipeline_walkthrough.png`, `_build_walkthrough_fig` returns 8 panel statuses (G `rendered`, others `ok`) + 8 fig axes; CLI `main(...)` returns 0 and writes the PNG. (4) SELF-SKIP: a missing step07 panel AND no VSI staging → a UserWarning per skip (D `missing`, G `skipped`), NO crash, PNG still produced with 8 axes. All 11 pass; full fishsuite suite 136 passed. |
| `tests/test_cli_postrun.py` | NEW (2026-06-18). 16 GPU-free tests for the post-run CLI layer (`cli.py` `backfill`/`walkthrough`/`postrun`) via Click's `CliRunner`. Proves WIRING only (the heavy `backfill_run`/`build_walkthrough_figure` are monkeypatched on `cli._backfill_run`/`cli._build_walkthrough_figure`, so NO VSI/GPU): the three subcommands are registered + appear in `--help`; `backfill --help` states CPU/GPU + mask/spot reuse + documents every toggle; default dispatch sends `do_null_draws/radial/montage=True`, `seed=0`, `staging/input=None` (auto-detect); the `--no-*` toggles + `--seed` flow through; `--staging` is forwarded; friendly errors (missing run dir, no-staging ValueError -> message contains `--staging`, FileNotFoundError -> mentions `per_image_summary`/"finished") never print a Traceback; walkthrough default `image_key/out_path=None` + `--image`/`--out` forwarded; `postrun` runs backfill THEN walkthrough (order asserted), prints both step names + the produced PNG, continues + exits non-zero when a step fails, and forwards `--staging` to both. All 16 pass. |
| `tests/test_cli_singlecell_pixelpattern.py` | NEW (2026-07-04). 10 GPU-free tests for the `singlecell` + `pixelpattern` post-run CLI layer via Click's `CliRunner` (heavy `singlecell_run`/`pixelpattern_run` monkeypatched on `cli._singlecell_run`/`cli._pixelpattern_run`, so NO image/GPU). Proves both subcommands are registered + in `--help`; help states CPU (+ GPU/mask reuse for pixelpattern) + documents `--abundance-col`/`--group-a`/`--group-b`/`--secondary-match`/`--no-figures`/`--no-excel`/`--staging`; default dispatch sends the auto-detect defaults (`abundance_col/group_a/group_b/secondary_match=None`, `exclude_secondary=True`, figures+excel ON, out_subdir defaults); the overrides + `--no-*` toggles + `--include-secondary` flow through; `--staging` forwarded to pixelpattern; friendly errors (missing nuclei_metrics.csv -> message mentions `nuclei_metrics`; missing masks -> mentions `mask`; RuntimeError "no nuclei processed") never print a Traceback. All 10 pass. |
| `tests/test_repro.py` | NEW (2026-06-10). 6 GPU-free unit tests for `core/repro.py`: `set_global_seeds` returns a bool dict with the expected keys; same seed → IDENTICAL NumPy AND Python `random` draw sequences (and a deterministic `default_rng`); `set_global_seeds` NEVER raises even when `import torch` is monkeypatched to raise (torch keys stay False); `write_versions_txt` produces a non-empty file containing the seed + "python" + "numpy"; `write_command_log` produces a non-empty file containing the seed + "argv" + the extra k:v; `write_run_metadata` writes both files. All pass. |
| `tests/test_qc_flags.py` | NEW (2026-06-10). 4 GPU-free unit tests for `core/qc.py::compute_qc_flags` using a `SimpleNamespace` fake `res` + a real `FishsuiteConfig`: an over-exposed (full-scale uint16) dapi+rna image with 0 nuclei + 0 spots → `qc_frac_saturated_*` > 0, `qc_flags` contains saturated_dapi/saturated_rna/low_nuclei/zero_spot, `qc_pass` False, and absent rna2/antibody planes emit NO column; a clean mid-gray image with ≥5 nuclei + spots → all frac_saturated 0.0, `qc_flags` "", `qc_pass` True, finite focus score; rna_protein with an `antibody_2d` plane emits `qc_frac_saturated_antibody`; garbage/empty `res` returns a dict without raising. All pass. |
| `tests/test_threshold_intensity.py` | NEW (2026-06-02). 10 GPU-free unit tests for the thresholded-RNA-intensity-in-compartments feature. Covers the core `metrics.compute_thresholded_compartment_intensity` math on a synthetic 4x4 RNA plane + disjoint nucleus/cytoplasm masks (known total/mean/area/fraction per compartment, `>=`-inclusive at the exact floor, floor-respected when raised, NaN-but-area-0 for no/zero/negative floor, empty-mask -> NaN fraction), and the floor-resolution precedence in `rna_only._resolve_thresh_intensity_floor` (DEFAULT = spot floor via `manual_rna_min`; explicit `rna_intensity_threshold` pin overrides; runner-forwarded `analysis_floors` used as default; rna2 channel uses rna2-specific sources; NaN when nothing set). |

## Docs

| Path | Description |
|---|---|
| `F:\Image Analysis Work\MIAT-QKI-Coloc\UD\PIPELINE_FIX_ORIENTATION_2026-06-05.md` (out-of-repo) | NEW (2026-06-05). MASTER-CONTROL orientation map for fixing 7 known rna_protein (MIAT×QKI) pipeline issues in the fishsuite BASE CODE: pinpoints file+function+line ranges for (1) the QKI/antibody spot-detection hack → proposed `detect_antibody_spots` flag in schema.py `FociCfg` (does NOT exist yet), (2) step05/06 walkthrough threshold panels keyed to the DISPLAY floor in output.py `save_walkthrough_bundle_rna_rna._channel_steps`, (3) obsolete step08/09 spot-pairing/active-TSS panels, (4) downstream coloc superplot axes hard-clamped to 0–1 in single_condition_plots.py `_superplot_into_axes`, (5) heavy condition outline in `_bar_spot_count_bin_composition` (COND_EDGE_LINEWIDTH=2.5), (6) nucleolus off in presets (core/nucleolus.py wired via rna_only.py 915–971), (7) antibody display-floor authority = `manual_antibody_*` not `manual_rna2_*`. Includes a "where we are" data-state summary of the two trusted runs (g2 + ALLARMS PLAIN strictMIAT DISPLAYFIX 2026-06-05) and a 4-group fix order. READ-ONLY analysis; no code changed. |
| `docs/END_TO_END_RESULTS.md` | First-run summary on Brian's H9 dataset (Phase 0). |
| `docs/PARITY_REPORT.md` | NEW (2026-05-11). Column-by-column and dir-by-dir comparison between fishsuite output and the Fiji pipeline reference (`F:\Image Analysis Work\H9-Analysis\`). Tracks where fishsuite matches Fiji, what it adds, and the small set of intentional divergences. |
| `docs/fiji_vs_fishsuite_audit.md` | NEW (2026-05-12). Z-stack handling + per-image numerical audit of fishsuite vs Fiji on the H9 MIAT-KD-ASO run. Documents that both pipelines do 2D-autofocus spot detection (not 3D), identifies the H9 preset z-range (20-30) as the dominant divergence vs Fiji's 5-15, and lists rolling-ball preprocessing + intensity-column meaning as secondary gaps. |
| `docs/IMPLEMENTATION_LOG.md` | Implementation diary across phases. |
| `docs/PHASE0_STATUS.md` | Phase-0 status snapshot. |
| `docs/gui_light.png` | NEW (2026-05-13). Offscreen-rendered screenshot of the GUI in Light theme — used as visual contrast evidence for the 2026-05-13 readability pass (text-as-tofu boxes are an offscreen-Qt artifact; colours and layout are accurate). |
| `docs/gui_dark.png` | NEW (2026-05-13). Offscreen-rendered screenshot of the GUI in Dark theme — pair to gui_light.png. Confirms status dots stay visible against the dark tab bar and form-field text contrasts against the input background. |
| `README.md` | Top-level package README. 2026-06-18: added a "Post-run figures & tables" section (the `postrun`/`backfill`/`walkthrough` commands, CPU-only) pointing at POSTRUN_UTILITIES.md. |
| `POSTRUN_UTILITIES.md` | NEW (2026-06-18). Beginner-friendly guide to the post-run utilities for a non-expert user: what "post-run" means (extra figures/tables from a FINISHED run dir, reusing saved masks+spots), the GPU clarification (GPU only for the main run; `backfill`/`walkthrough` are CPU-only and new runs with the coloc preset flags ON emit everything themselves so backfill becomes unnecessary), copy-pasteable `fishsuite postrun`/`backfill`/`walkthrough` examples with a realistic run path, the output files each produces + where they land (coloc_null_draws.csv, coloc_null_summary.csv, coloc_radial_profile.csv, the QKI montage, the 8-panel walkthrough PNG), the common options, and a power-user back-compat note on the `python -m ...` entry points. 2026-07-07: added a "MIAT × QKI association metrics" section documenting the new footprint-based per-spot/per-nucleus/per-image columns (`qki_at_miat_footprint`, `miat_footprint_area_px`, `qki_footprint_enrichment`, `qki_assoc_ratio_continuous`, `qki_assoc_ratio_gated_<floor>`, `coloc_moc`, `coloc_icq`, `qki_at_miat_foci_enrichment`), the EXACT half-max (FWHM) footprint definition + disk fallback, and the enabling flags (`foci.compute_footprint_enrichment`, `foci.assoc_qki_floor`). |
| `THRESHOLD_INTENSITY_FEATURE.md` | NEW (2026-06-02). Design + usage note for the thresholded-RNA-intensity-in-compartments feature: metric definition, the new `rna_thresh_*` / `rna2_thresh_*` per-nucleus columns + per-image `mean_*` rollups, the `output.rna_intensity_threshold` / `rna2_intensity_threshold` config fields and their default-to-spot-floor behavior, which modes/channels are covered (rna_only / rna_rna / rna_protein), and how it differs from spot-based intensity (`rna_spot_total_peak_intensity`) and raw-sum intensity (`sum_rna_intensity`). |
