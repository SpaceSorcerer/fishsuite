# fs-spotsize — pending file_map entries (2026-09-04 20:15)

NOTE: these five entries are ALREADY in `E:\Claude\fishsuite\file_map.md`, appended and
committed in `23a08b3` at ~17:40, BEFORE the "no direct file_map edits" correction
arrived. Listed here so the merge does not duplicate them. Nothing else of mine touches
any file_map.

| Path | Description |
|---|---|
| `E:\Claude\fishsuite\src\fishsuite\core\spot_size.py` | Real per-spot size measurement: a 2-D Gaussian plus constant background fitted around every detected spot by batched Levenberg-Marquardt, with adaptive window escalation for spots wider than their crop and a `size_fit_flag` naming every rejection. Also converts a stored `miat_footprint_area_px` to um^2 and an equivalent diameter, and rolls medians up per nucleus and per image. |
| `E:\Claude\fishsuite\src\fishsuite\core\sizefit_backfill.py` | CPU backfill of that fit onto a finished run. Re-opens each source image, re-extracts the recorded z-plane, fits every spot, and writes `spot_metrics_sizefit.csv`, `sizefit_per_nucleus.csv`, `sizefit_per_image.csv`, `sizefit_command.log` and `sizefit_versions.txt` beside the run's tables without modifying them. |
| `E:\Claude\fishsuite\tests\test_spot_size_fit.py` | Recovery of known sigma within 5 % on synthetic Gaussians, agreement with `scipy.optimize.curve_fit`, adaptive-window recovery of an over-wide spot, the rejection flags, footprint unit conversion, a guard that the schema and function window defaults agree, and a test documenting the saturation of the legacy moment estimator. |
| `E:\Claude\fishsuite\docs\SPOT_SIZE_FIT_2026-09-04.md` | Why `spot_diameter_um` / `spot_fwhm_px` / `spot_area_px` are near-constant, with exact file and line numbers and a measured saturation table; the fit's model, window choice, columns and flags; and the backfill command. |

Per-run outputs written by the backfill (one set per run directory, all additive):
`spot_metrics_sizefit.csv`, `sizefit_per_nucleus.csv`, `sizefit_per_image.csv`,
`sizefit_command.log`, `sizefit_versions.txt`.
