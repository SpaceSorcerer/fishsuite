# Pending file_map entries - WP2 engine/report work, e081d71..ce4c609 (2026-09-05)

Format: full path + 1-2 sentences. Append verbatim at merge time.
Branch `fix/per-image-nuclear-columns` was fast-forwarded into `main`; `main` is
now `ce4c609`.

## Engine

- `E:\Claude\fishsuite\src\fishsuite\core\modes\rna_rna.py` - The per-image
  nuclear counters now sum `nuclei_df_all` rather than the sampled `nuclei_df`,
  so `nuclear_spots_*` and `cytoplasmic_spots_*` cover every nucleus and match
  the image-wide `total_spots_*` they are divided by. Previously
  `nuclear + cytoplasmic != total` and `frac_nuclear_*` was low by the sampling
  ratio on any run using fixed-N sampling with `apply_to_rollups`.
- `E:\Claude\fishsuite\src\fishsuite\core\repro.py` - New `engine_git_commit()`
  returns the 40-character SHA of the checkout the module runs from, suffixed
  `-dirty` when the tree has uncommitted changes and `"unknown"` outside a repo
  or when git is unavailable; it is written to `versions.txt` as
  `fishsuite_git_commit` immediately after `fishsuite_version`. Runs previously
  recorded only the package version, so which engine produced a run was
  evidenced solely by logs outside the run directory.

## Report

- `E:\Claude\fishsuite\src\fishsuite\report\aggregate.py` - Two changes. The
  engine's own per-nucleus column now wins over the recomputed one (fs-report's
  fix, absorbed): on an ungated run both frames carried the pairing columns,
  pandas suffixed them `_x`/`_y`, and `paired_fraction_rna1_at_0p3um` and
  `median_nn_distance_rna1_um` resolved as ABSENT. Also derives the
  scope-matched densities `rna1_nuclear_spots_per_um2` and
  `protein_spots_per_um2`, preferring the engine's own
  `nuclear_spot_density_per_um2*` columns and computing from the counts only
  when absent.
- `E:\Claude\fishsuite\src\fishsuite\report\endpoints.py` - Adds
  `protein_spots_per_um2` (exploratory, `excluded_from_holm = "proxy for
  absolute partner level"`, mirroring `protein_spots_per_nucleus`) and
  `rna1_nuclear_spots_per_um2`. The pre-existing `rna1_spots_per_um2` /
  `rna2_spots_per_um2` keep their values but their unit and note now state the
  scope mismatch: TOTAL puncta over NUCLEAR area is a count normalised by
  nuclear size, not a concentration.
- `E:\Claude\fishsuite\src\fishsuite\report\figures.py` - `FigureContext` takes
  `nucleus_filter`, `n_nuclei_all` and `n_nuclei_after_nucleus_filter`, and the
  on-figure filter line states the filter actually applied instead of a
  hardcoded "every segmented nucleus, no post-hoc nucleus filter". An
  unrecognised value prints verbatim rather than defaulting to "all".
- `E:\Claude\fishsuite\src\fishsuite\report\build.py` - Passes those three
  values from the aggregate into `FigureContext`.

## Scripts

- `E:\Claude\fishsuite\scripts\coloc_standard_panel.py` - Now tracked. Reads the
  arm from the first of `group` then `line` present in the report's
  `per_well.csv`, because `fishsuite report` writes `group` and the old
  `line`-only gate silently fell back to the condition prefix and refused
  `--arm-order`; it warns naming both columns when neither is found. New
  `OBJECT_FRACTION_PREFIXES` constant fixes the six `paired_frac_*` rows that
  were labelled `role = pixel, descriptive` and `unit = ICQ -0.5..+0.5` because
  the test only matched `frac_`; the values were always correct.

## Docs

- `E:\Claude\fishsuite\docs\PER_IMAGE_NUCLEAR_COLUMNS_IMPACT_2026-09-05.md` -
  Consumer trace for the per-image nuclear-column defect: which code reads those
  columns versus re-derives from `nuclei_metrics.csv`, and a verdict per
  delivery. Only `DELIVERY_MIAT_QKI_REVISED_2026-09-03` is affected, 42 of 44
  images; `fishsuite report` is not affected because it reads per-nucleus
  columns only.
- `E:\Claude\fishsuite\docs\REPORT_FILTER_LINE_IMPACT_2026-09-05.md` - Survey of
  all 13 `report_*` directories under `F:\Image Analysis Work\` with the
  `nucleus_filter` each used. All 13 used `all`, so all 393 SVGs carrying the
  filter line are correct and the three deliveries are unaffected; the fix is
  preventive.

## Tests

- `E:\Claude\fishsuite\tests\test_per_image_nuclear_columns_2026_09_05.py` - 15
  tests that the per-image nuclear counts equal the sum over ALL nuclei, that
  `nuclear + cytoplasmic == total`, that `frac_nuclear` matches its own
  numerator and denominator, and that enabling sampling does not move the
  image-level counts, with a guard that the fixture really samples a subset.
- `E:\Claude\fishsuite\tests\test_report_filter_line_2026_09_05.py` - 10 tests,
  one per `nucleus_filter` value plus wording, footer consistency and the
  default-argument case.
- `E:\Claude\fishsuite\tests\test_group_column_and_engine_commit_2026_09_05.py`
  - 11 tests covering the panel's `group`/`line` resolution and the recorded
  engine commit, including that it says `"unknown"` outside a repo and never
  raises.
- `E:\Claude\fishsuite\tests\test_density_endpoints_and_panel_labels_2026_09_05.py`
  - 18 tests for the two new per-area endpoints, the scope-mismatch labelling of
  the existing ones, and the six object-fraction rows' `role`/`unit`.
- `E:\Claude\fishsuite\tests\test_report_peak_gate_and_pairs.py` - Gains
  fs-report's regression test that a derived column never shadows the engine's
  own and that neither pairing endpoint lands in the absent set.
