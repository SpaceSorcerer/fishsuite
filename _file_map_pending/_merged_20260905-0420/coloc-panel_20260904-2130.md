## Pending file_map entries — tool repo (agent: coloc-panel, 2026-09-04 21:30)

Target section: `## Scripts` table, same row format as `scripts/render_pub_images.py`.

- `scripts/coloc_standard_panel.py` - NEW (2026-09-04, untracked). Absolute path `E:/Claude/fishsuite/scripts/coloc_standard_panel.py`. CPU-only post-run tool that adds the field-standard colocalization readouts to a COMPLETED `rna_rna` / `rna_protein` run, alongside (never instead of) the engine's rotation null. Per-nucleus Pearson r, Li ICQ and Manders M1/M2; an object-based called-colocalized fraction whose sampling region is each punctum's EXACT half-max footprint (the engine's own `qki_at_miat_footprint` / `miat_footprint_area_px`; the footprint shape is rebuilt only so the shuffle can translate it and is verified against those columns, max |d area| 0.000 over 2701+23829+2495 puncta across three runs); a within-nucleus shuffle control that translates the same footprint shape and requires every translated pixel to stay inside the nucleus; pooled-pixel cytofluorograms; intensity line profiles across representative nuclei; and QC overlays colouring every punctum by its call. `--anchor rna1|partner|both` measures both directions when the run detected partner spots, and adds spot-to-spot pairing at the run's own pair distance with its own shuffle. `--call-threshold auto|costes|batch`: auto uses the run's single batch median+k*MAD threshold as the PRIMARY call whenever Costes converges in fewer than `COSTES_MIN_CONVERGENCE_PCT` (90%) of biological nuclei, because below that a Costes-with-fallback column mixes two conventions and its arm mean tracks the convergence fraction; the other variant is always kept as the sensitivity twin. Arbitrary arms via `--arm-map PATTERN=ARM` and `--arm-order` (reference first; contrasts report test minus reference). Reads the run read-only, never re-detects spots, never writes into `--run`, refuses to run on a run made without `foci.compute_footprint_enrichment` rather than substituting a disk. Emits `coloc_standard_{per_nucleus,per_fov,per_well,contrasts,biological_questions}.csv`, `coloc_standard_panel.xlsx` (README / per_nucleus / per_FOV / per_well / contrasts / line_profiles / overlays_index / biological_questions), nine 600-dpi PNG + editable-text SVG figures, `overlays/`, `command.log`, `versions.txt`, `FIGURE_INDEX.md`. ONE statistical test only (Welch t on well means of the primary object endpoint, with Hedges g and an MDE computed from the actual well counts); pixel metrics are descriptive with CIs. Flags zero-within-arm-variance contrasts so a degenerate p is never quoted as evidence strength. |

Note for the merger: this row REPLACES the `scripts/coloc_standard_panel.py` row I added directly to `E:\Claude\fishsuite\file_map.md` earlier today (before the 16:30 pending-file rule). See the "already edited" note in my report.
