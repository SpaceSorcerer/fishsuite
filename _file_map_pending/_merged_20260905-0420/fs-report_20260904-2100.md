# Pending file_map entries from agent fs-report (20260904-2100)

Format: full path + 1-2 sentences. Append verbatim at merge time.

NOTE: this agent ALSO appended directly to the project file_map.md earlier tonight, before the pending-file rule was issued. Those appends are listed at the bottom of this file so the merge does not duplicate them.

## New in fishsuite (second and third pass, 2026-09-04)

- `E:\Claude\fishsuite\src\fishsuite\report\peak_gate.py` - Applies a peak-intensity floor to a finished run's spots and re-derives the per-nucleus counts. fishsuite's own spot floor is post-detection, so this reproduces a gated run without re-detecting. Boundary is at or above the floor; the nuclear-fraction denominator is in-nucleus plus in-cytoplasm, matching the engine; columns a floor makes stale (above-floor intensities, Manders, the rotation-null enrichments) are NAMED in the workbook rather than silently carried at their pre-gate value.
- `E:\Claude\fishsuite\src\fishsuite\report\provenance.py` - Writes `SOURCE_RUN.md`, the `00_SOURCE_FISHSUITE_RUN` junction (via `cmd /c mklink /J`, never `ln -s`) and `provenance\source_run\` copies into every report folder.
- `E:\Claude\fishsuite\tests\test_report_peak_gate_and_pairs.py` - Pins the peak-gate boundary, the nuclear-fraction denominator, the naming of stale columns, and that five groups give ten pairwise contrasts with the Tukey adjustment fitted over all five (k=5) at both the field and the well level.
- `E:\Claude\fishsuite\tests\test_report_size_provenance_wells.py` - Pins that the engine's moment-based punctum-size estimator SATURATES past about 3 px of true width (measured against synthetic Gaussians), that the footprint conversion uses the run's own voxel size, that a well-recovery regex missing any biological image is refused, and that every column alias names the same measurement as its primary.

Changed the same day: `report\endpoints.py` (punctum footprint area as the PRIMARY size endpoint; the moment-estimator columns demoted to descriptive-only with the measured saturation stated; `alt_columns` so one endpoint covers the `protein_*` and `rna2_*` spellings); `report\aggregate.py` (`well_from_image` regex, `nucleus_filter`, all-pairs contrasts, well-level Tukey alongside the field-level one, and `well_pooled_mean_of_nuclei` beside `well_mean_of_field_values`); `report\stats.py` (Tukey fitted over EVERY group in the design, the pair read out afterwards; a two-group fit inside a five-group design understated the adjusted p); `report\figures.py` (locked imaging pair WT `#595959` / QKI-KO `#D67AE5`, overridable via `group_colors(order, overrides)`); `report\build.py` and `cli.py` (`--groups-file`, `--group-order`, `--well-from-image`, `--peak-floor`, `--caveat-file`, `--nucleus-filter`, `--all-pairs`).

---

## Already appended DIRECTLY by this agent earlier tonight (do NOT duplicate at merge)

- `E:\Claude\fishsuite\file_map.md` - two appended sections, "Added 2026-09-04 - condition groups and the `fishsuite report` layer" and "Added 2026-09-04 (second pass) - punctum size, source-run linkage, groups files".
- `F:\Image Analysis Work\RNASEH2B_BIN1introns_2026_08_25\file_map.md` - two appended sections, the `fishsuite report` proof output and the punctum-size corrected reading.
- `F:\Image Analysis Work\BIN1_Introns_Exons_RNA-FISH_Analysisi\file_map.md` - one appended section, "Added 2026-09-04 - fishsuite report on the harmonized T220 run".
- `F:\Image Analysis Work\MIAT_QKI_Coloc_2026_08_25\file_map.md` - one appended section, "Added 2026-09-04 - fishsuite report on the canonical run".
- `F:\Image Analysis Work\QKI_BIN1introns_2026_08_25\file_map.md` - one appended section, "Added 2026-09-04 - fishsuite report on both arms".

All five were verified after writing: 0 control characters, 0 replacement characters,
trailing newline present, no stray .tmp file beside them. The entries ABOVE are the
NEW ones and supersede the earlier text where they overlap, because the reproduction
results and the coloc-panel outcomes were not known when the direct appends were made.
