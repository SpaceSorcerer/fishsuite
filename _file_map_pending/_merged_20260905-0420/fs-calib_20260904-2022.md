# Pending file_map entries — fs-calib, 2026-09-04 20:22

Format: full path + 1–2 sentences. Merge into `E:\Claude\fishsuite\file_map.md`.

## Modified (commit 8e76cda)

- `E:\Claude\fishsuite\src\fishsuite\config\schema.py` — append: *2026-09-04 `FociCfg` gains four default-off options: `partner_anchored_null_sample_field` (`rna` / `dapi` / `both`, DAPI being a signal-free reference for the partner-anchored rotation null), `partner_anchored_null_nuclear_anchors_only` (restricts the partner constellation to in-nucleus anchors so observed and null share one support), and `rna_pedestal_normalize` + `rna_pedestal_stat` (per-image rna1 pedestal normalisation before LoG detection). Also carries fs-spotsize's `compute_size_fit` / `size_fit_window_px`, whose consuming code landed in 23a08b3 without them.*
- `E:\Claude\fishsuite\src\fishsuite\core\modes\rna_rna.py` — append: *2026-09-04 partner-anchored rotation null generalised to a loop over sampled fields on disjoint RNG streams (rna1 `seed+303`/`+606`, dapi `seed+909`/`+1212`), so a `both` run reproduces an `rna`-only run bit-for-bit; optional in-nucleus anchor restriction emitting `n_partner_anchors_at_rna2_spots`; `run_one` accepts `rna_pedestal_factor` and scales the rna1 plane before detection only, restoring raw `intensity_peak` and adding `intensity_peak_normalized`; gated pedestal provenance in `thresholds`. Also repairs the per-spot size-fit emit to dispatch on value type so the string-valued `size_fit_flag` no longer hits `float()`.*
- `E:\Claude\fishsuite\src\fishsuite\core\modes\rna_protein.py` — append: *2026-09-04 forwards `rna_pedestal_factor` through to `rna_rna.run_one`.*
- `E:\Claude\fishsuite\src\fishsuite\runner.py` — append: *2026-09-04 new module-level pure `resolve_rna_pedestal()` returning the batch reference and per-image factors; the batch pre-pass now also records each image's in-nucleus rna1 median, and the per-image loop forwards the factor to `rna_rna` / `rna_protein`. The feature requires `pixel_coloc.threshold_scope: batch` and prints an explicit NOTE rather than inventing a reference without it.*

## New (commit 8e76cda)

- `E:\Claude\fishsuite\tests\test_dapi_reference_null_and_rna_pedestal.py` — NEW: *48 GPU-free tests for the DAPI-plane reference null, the in-nucleus anchor restriction and the rna1 pedestal normalisation, including a spy proving the DAPI family hands the DAPI plane to the existing `_rotation_null_for_nucleus`, a bit-for-bit check that `both` does not perturb the rna1 draws, and a test documenting that `only_nuclear_spots` never filters the spot table.*
- `E:\Claude\fishsuite\docs\ENGINE_CHANGE_2026-09-04_dapi_reference_null_and_rna_pedestal.md` — NEW: *change note for the three 2026-09-04 options, with the real-data no-op proof table, the reason each option exists, and the `only_nuclear_spots`-is-inert footgun.*

## Uncommitted, analysis-side scratch (not in the repo)

None. All repo changes above are committed as `8e76cda`, not pushed.
