# Pending file_map entries — fs-calib, 2026-09-05 02:30

Format: full path + 1–2 sentences. Merge into `E:\Claude\fishsuite\file_map.md`.
Adds to `fs-calib_20260904-2022.md`.

## Modified (commit ff5a53b)

- `E:\Claude\fishsuite\src\fishsuite\config\schema.py` — append: *2026-09-05 `FociCfg.rna_pedestal_factor_max` (default None = no clamp): an upper bound on the per-image pedestal factor, so a field brighter than the batch reference is still scaled down onto it while a dimmer field is left alone rather than amplified.*
- `E:\Claude\fishsuite\src\fishsuite\core\modes\rna_rna.py` — append: *2026-09-05 the per-image pedestal factor is clamped to `foci.rna_pedestal_factor_max` when set; `thresholds.csv` gains `rna_pedestal_factor_raw`, `rna_pedestal_factor_max` and `rna_pedestal_factor_clamped` beside the applied `rna_pedestal_factor`, all gated on `rna_pedestal_normalize`, so a clamped run records what an unclamped one would have applied.*
- `E:\Claude\fishsuite\tests\test_dapi_reference_null_and_rna_pedestal.py` — append: *2026-09-05 eight further tests for the factor clamp: the cap bites above it, leaves values below it untouched, treats a value exactly at the cap as unclamped, is inert when unset, emits no columns when the pedestal feature is off, and reproduces the un-normalised spot positions exactly when an up-scaling factor is capped at 1.0. 83 tests in the file's two suites.*
