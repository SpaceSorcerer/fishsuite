"""Vendored third-party-to-this-project code, copied verbatim.

Everything under this package is a **verbatim copy** from a separate
repository. See ``PROVENANCE.md`` in this directory for the source repository,
the exact commit, per-file SHA-256 checksums, and the single documented
deviation.

Do not edit files in this package. To change behaviour, wrap or subclass from
``fishsuite/core/``. Editing a vendored file breaks the checksum record that
lets a reader verify the algorithm is unchanged from the version that produced
previously published results.

Lint and format tooling is configured to skip this directory for the same
reason.
"""
